# Mac 扫码登录 PRD 精简设计

## 1. 目标

将现有 536 行 PRD 压缩到约 180–220 行，保留开发和测试必须的信息。只调整文档结构和表达，不改变已确认的产品逻辑、Demo、截图与固定链接。

## 2. 精简原则

- 只写最终规则，不保留 V1.0、V1.1、V1.2 的层叠说明和删除线历史。
- 同一规则只出现一次；验收表不再重复完整交互说明。
- 删除价值分析、过程性问题总结、自检记录、模拟评审和泛化描述。
- 去掉“硬伤、自动补充、优先执行”等过程语言，改为直接、可执行的产品表述。
- 保留会影响开发实现、异常处理、数据安全或测试结论的规则。
- 不新增页面、弹窗、Toast、状态或流程。

## 3. 目标结构

### 一、版本信息

版本表只保留一条创建记录：`2026.07.25｜V1.0｜郑群超｜创建文档｜备注为空`。

### 二、背景、目标与范围

合并现有背景、目标、用户场景、价值和范围，只保留：

- 要解决的问题。
- App 扫码登录 Mac 的目标。
- 本期范围和不做事项。
- 二维码有效期、账号和设备绑定、平台继承、独立设备会话、最终结果反馈等核心规则。

### 三、核心流程

保留一条动作链和一张流程图：

`Mac 展示二维码 → App 扫码 → App 确认 → 服务端授权 → Mac 领取并确认 → 双端返回结果`

### 四、功能设计

#### 4.1 功能入口与 Demo

保留 Demo 固定链接和模块摘要，不重复页面细节。

#### 4.2 C 端页面与状态

所有 App、Mac 页面和状态统一放入一张表，不再拆分到 4.4、4.5 和验收章节。

按 `to-prd` 主模板使用三列结构：

|模块名称|图示|展示&交互说明|
|---|---|---|

模块名称合并端和页面或状态并加粗。每行第三列统一使用 `<br>` 换行，按以下加粗小标题组织：

`**① 触发条件：**` → `**② 页面展示：**` → `**③ 操作流程：**` → `**④ 状态反馈：**` → `**⑤ 异常处理：**`

展示和交互必须位于同一单元格，不再拆成多个表格列。

必须包含以下行：

1. Mac 登录页－等待扫码。
2. App“我的”页－扫码入口。
3. App－相机权限申请与引导。
4. App－扫码页。
5. App－登录确认。
6. Mac－等待 App 确认。
7. App－授权中与待领取。
8. App－登录成功。
9. App－授权失败。
10. App－取消授权。
11. App－二维码过期。
12. Mac－登录成功并返回 `returnTo`。
13. Mac－授权失败。
14. Mac－登录取消。
15. Mac－二维码过期。

13 张页面图全部放在对应表格行；一张流程图保留在第三章。

#### 4.3 服务端关键规则

使用一张状态与处理表，仅保留：

- `waiting_scan`、`pending_confirm`、`authorizing`、`ready_to_claim`、`used` 及失败终态。
- 二维码 2 分钟有效期和确认、过期、取消的并发顺序。
- 服务端账号关系为平台继承真值源。
- Mac 使用独立设备会话，不复制 App Token。
- 会话先创建为不可鉴权状态，全部成功后原子激活；失败时拒绝鉴权并异步回收。
- 30 秒幂等领取、Mac 持久化 ACK、`ready_to_claim` 不提前提示成功。

删除实现过程说明、重复协议描述和责任边界长表。

### 五、非功能需求

只保留性能、兼容性、安全、弱网与降级，每项写清验收方式。

### 六、埋点

合并事件与关键参数，删除可由公共参数自动获取或没有决策用途的字段。

### 七、发布与指标

保留灰度、回滚、成功率、授权失败率和安全监控；删除运营素材与无实际配置事项。

### 八、验收与待确认项

保留 P0 核心链路、四类结果态、Toast 去重、唯一刷新按钮、`ready_to_claim` 边界、无效 `returnTo`、弱网与并发用例。验收表引用 4.2、4.3 的最终规则，不再次复制整段说明。

已确认事项直接写入正文；只保留真实未决项。

## 4. 保留与删除边界

|处理|内容|
|---|---|
|保留|最终页面、状态、文案、操作反馈、异常处理、会话安全、埋点、灰度、回滚和验收|
|合并|背景与目标、授权中与待领取、双端结果、事件与参数|
|删除|价值分析、历史规则、删除线、版本增补章节、重复状态表、过程性问题总结、自检、模拟评审|

## 5. 验证标准

- 总行数控制在 180–220 行；超过时继续删除重复内容。
- 4.2 覆盖全部 15 个 C 端页面或状态行。
- 4.2 表头为三列，15 个模块名称均加粗；每行包含 5 个加粗小标题和 `<br>` 换行。
- 13 张页面图均位于 4.2 表格；流程图位于第三章。
- 13 张页面图和 1 张流程图分别引用一次，Demo 与全部图片继续使用固定提交 SHA。
- 不残留本地路径、浮动分支链接、删除线、V1.1/V1.2 增补章节或模拟评审。
- 自动验证继续通过，产品行为与当前 Demo 一致。

## 6. 快速评审结论

- 用户视角：页面和状态集中后，可直接定位 App 与 Mac 的最终表现。
- 开发视角：服务端关键状态和安全边界仍保留，不会因篇幅压缩产生实现歧义。
- 业务视角：删除论证和过程记录，不影响范围、指标和上线判断。
