# APP MODS 已安装筛选子 Tab 实施计划

**目标：** 将已安装页“全部 / 可更新”改为左对齐子 Tab，刷新固定在同一行右侧；横屏整行位于标题下方第二行。

## 1. Demo

- 用两个 `role="tab"` 按钮承载“全部 / 可更新”，复用 `installedFilter` 状态。
- 子 Tab 左对齐，刷新右对齐。
- 竖屏位于主 Tab 下方；横屏位于标题栏下方第二行。

## 2. 验证

- 校验两个子 Tab 的文案、默认态和切换结果。
- 校验横竖屏子 Tab 左对齐、刷新右对齐、列表紧跟筛选行。
- 重新生成 10 张截图并重点检查 03、07、09、10。

## 3. PRD 与发布

- 更新已安装页、Steam 个人中心和截图验收说明。
- 先发布 Demo 与截图，再用固定 40 位提交号更新 PRD 图片和在线 Demo 地址。
- 逐张验证图片返回 `200 image/png`。
