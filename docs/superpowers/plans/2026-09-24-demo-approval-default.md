# Demo 默认审核通过与常驻切换

用户明确要求：默认审核通过；审核状态按钮常驻；沿用 developer-platform-demo 分支及官方 HTML 的 P01-01 入口。

实现：首次进入已登录 Demo 使用审核通过预览，不修改账号保存的认证资料。右下角审核状态按钮独立于页面数据按钮，所有后台页面可切换未提交、审核中、通过、未通过及暂停。刷新回到默认通过，恢复已保存状态仍可用。

验证：覆盖旧账号、刷新、跨页切换、游戏/厂商/渠道导航、登录、窄屏及现有渠道流程。推送同分支并清除官方 HTML 的 CDN 缓存，检查公网与本地一致。
