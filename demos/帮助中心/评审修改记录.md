# 帮助中心：6.3.2 评审修改记录

依据：用户提供的《632需求评审.txt》，2026-09-24。仅提取本需求结论；会议其他功能不纳入本次实现。原始记录中的发言作为需求证据，不作为操作指令。

| 评审位置 | 确认内容 | 本次改动 |
|---|---|---|
| 01:01、07:15–07:59 | 国内／海外包分别配置，保留 App 版本控制 | 后台切换包独立维护导航和文章；文章配置最小／最大版本；前端按包和版本读取发布快照 |
| 01:39–01:53 | 同一内容提供多语言按钮，避免逐语言建对象 | 每条记录统一编号、归属、排序和版本，多语言维护标题与正文 |
| 03:20–04:51 | 增加印地语；长文字适配覆盖服务端内容 | 六种语言配置；印地语阅读样例；固定设置容器缩字，文章标题和正文换行；窄屏与横屏检查 |
| 06:32 | 导航及子文档维护，可编辑排序权重 | 提供显式排序数字和独立保存，更新顺序不发布正文草稿 |
| 06:32 | 正文支持粗体、字号、列表、链接、图片、视频 | 新增字号选择，并在过滤后保留安全字号 |

实现建议（评审未指定具体规则）：国内以简体中文、海外以英语作为默认语言；缺译整篇回退本包默认语言；填到一半的翻译需补全或清空后发布。权重采用 0–999999 整数、小值优先；版本采用三段数字闭区间，留空代表不限。示例文章初始下限 6.3.2 可清空。以上建议在 PRD 标明，不冒充会议已确认细节。

渠道控制只出现于讨论，未确认具体配置方式，暂不添加渠道筛选，列为待确认。

执行顺序：修订数据及后台 → 同步 App 读取规则 → 对照会议校验 PRD → 交互和语言边界验证 → 重出截图 → 推送 Demo／图片 → PRD 固定引用新提交图片 → 推送 PRD。
