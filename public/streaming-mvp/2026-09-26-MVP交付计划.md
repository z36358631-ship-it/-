# 串流MVP摘要与双端Demo交付计划

沿用用户已确认的一期方案；本轮收敛范围并补齐已有PRD规定的交互。

1. 将既有必需能力归纳为9组，输出一期MVP功能摘要.md。
2. 基于demo.html原页面，补Mac全屏、退出与工具栏；不增加研究中的后置功能。
3. 从同一源生成mobile.html与mac.html两个离线单文件，新增mvp.html摘要入口。
4. 修正清空历史误纳一期、六组合开放条件以及三个错误图片引用。
5. 跑已有交互检查及全屏、双端入口、缩窄视口检查；渲染截图人工复核。
6. 顺序发布图片、取得固定SHA、更新图文、发布网站；核对HTTP及图片。

验收：不能自授信任、占用不抢控、多行不自动提交、撤权阻止传输完成、退出保留任务；全屏可使用工具和断开，Esc只退出全屏。原型不是远控引擎，不作像素复刻或真实性能通过声明。
