# 【Prd】《盖世游戏》兼容性诊断引导需求

## 一、版本信息

|时间|版本|变更人|主要变更内容|备注|
|---|---|---|---|---|
|2025.07.15|V1.0||创建文档||
|2026.07.07|V2.0||改版：去掉逐项检测流程，改为主动弹窗+引擎方案切换+问题反馈上报；后台新增弹窗文案配置模块（国内/海外+多语言）||

## 二、背景与目标

**背景：** 用户在使用盖世游戏启动PC游戏时，黑屏、闪退/崩溃、卡在启动/加载是最高频的3类问题。用户遇到问题后缺乏引导，只能自行摸索或放弃。V1.0的逐项检测+逐项修复流程过重，核心解决方案实际是切换PC启动引擎方案。

**目标：** 客户端识别到启动异常后主动弹窗，引导用户一键切换引擎方案，同时收集用户遇到的问题类型帮助定位根因，降低因兼容性问题导致的用户流失率。

**核心挑战：**
- 3种异常场景的识别准确性（黑屏需区分有声音无画面 vs 完全无响应）
- 引擎方案推荐需按芯片+游戏动态匹配，展示启动成功率
- 问题类型标签需支持后台配置+多语言

## 三、故事介绍

### 3.1 用户与运营场景

**场景1：横屏游戏中触发**
用户启动艾尔登法环后出现黑屏（有声音没画面），客户端识别到异常，自动在游戏画面上弹出诊断弹窗，展示3个可选引擎方案和启动成功率。用户选择"系统智能设置方案"（成功率94.4%），可选标记"黑屏"标签并描述"有声音没画面"，点击"切换方案"→ 二次确认 → 应用成功，重启游戏后正常进入。

**场景2：竖屏详情页闪退后**
用户玩博德之门3时闪退，重新打开APP进入游戏详情页，系统检测到上次异常退出，弹出竖屏弹窗推荐切换引擎方案。用户选择方案后确认切换，重启游戏恢复正常。

### 3.2 价值分析

- 降低流失：将"遇到问题就卸载"转化为"一步切换方案即可解决"
- 提升启动成功率：引导用户切换到成功率最高的引擎方案
- 数据沉淀：收集用户反馈的问题类型+描述，反哺引擎方案优化
- 降低客服压力：用户自助切换，减少人工介入

### 3.3 核心体验路径

- C端用户路径：游戏启动异常 → 弹窗展示 → 选择引擎方案 → 可选标记问题类型+描述 → 切换方案 → 二次确认 → 应用成功 → 手动重启游戏
- B端运营路径：查看问题上报列表 → 分析反馈类型分布 → 配置/调整异常类型标签

### 3.4 产品指标预测

- 弹窗展示后方案切换率 ≥ 50%
- 切换后游戏启动成功率 ≥ 85%
- 问题类型反馈填写率 ≥ 30%（可选项）
- 用户因兼容性问题7日流失率降低 15%
- 客服兼容性相关工单量降低 30%

### 3.5 路径规划

- V2.0（当前）：主动弹窗+引擎方案切换+问题上报+后台文案配置
- V3.0（预告）：根据问题反馈数据自动优化引擎方案推荐权重；接入崩溃日志自动关联

## 四、概要设计

### 4.1 模块设计

|模块|说明|
|---|---|
|C端-主动弹窗（横屏）|游戏内识别异常后弹窗，含引擎方案+问题反馈|
|C端-主动弹窗（竖屏）|闪退后在详情页弹窗，仅引擎方案选择|
|C端-二次确认|弹窗内内容替换为确认状态|
|B端-问题上报列表|查看用户上报的设备信息+反馈内容|
|B端-弹窗文案配置|配置异常类型标签，支持国内/海外+多语言|

### 4.2 详细设计（C端）

功能demo：`demos/兼容性诊断引导demo.html`

|模块名称|图示|展示&交互说明|
|---|---|---|
|横屏主动弹窗|demo场景①②③|**触发条件：** 客户端识别到黑屏/闪退崩溃/卡在启动加载3种场景后自动弹出。<br><br>**展示：**<br>- 蒙层覆盖游戏画面，居中弹窗<br>- 弹窗头部：⚠️图标 + 标题"检测到游戏启动异常" + 引导语"建议切换PC启动引擎方案，选择你遇到的问题帮助我们定位并识别"<br>- 引擎方案区：3个方案横排1行，每个方案显示方案名+启动成功率，单选，默认选中第1个<br>- 启动异常类型区：标题"启动异常类型（可选）"，4个标签可多选（后台配置），下方textarea输入框<br>- 底部：上方文案"切换后需重新启动游戏方可生效"，下方两个按钮各占一半"暂不处理"/"切换方案"<br>- 右上角✕关闭按钮<br><br>**交互：**<br>- 点击方案卡片 → 切换选中态（金色边框+radio填充）<br>- 点击类型标签 → 切换选中/未选中（可多选）<br>- 点击"切换方案" → 进入二次确认<br>- 点击"暂不处理"或✕ → 关闭弹窗，本次不再弹出<br>- 同一游戏24h内最多弹出2次|
|竖屏详情页弹窗|demo场景④|**触发条件：** 闪退后重新进入APP，打开该游戏详情页时弹出。<br><br>**展示：**<br>- 蒙层 + 垂直居中弹窗<br>- 弹窗头部：同横屏（标题+引导语），引导语简化为"建议切换PC启动引擎方案"<br>- 引擎方案区：3个方案纵向排列（1行1个），每个方案显示方案名+启动成功率<br>- 无问题反馈区（无类型标签、无描述输入框）<br>- 底部：两个按钮各占一半"暂不处理"/"切换方案"，无提示文案<br><br>**交互：** 同横屏逻辑|
|二次确认|demo确认态|**展示：**<br>- 弹窗内内容替换（隐藏header+body+footer），不叠加新弹窗<br>- 确认区居中展示：⚠️图标 + 标题"确认切换引擎方案？" + 描述"将切换为「方案名」方案（启动成功率 XX%），切换后需重新启动游戏方可生效" + 两个按钮"取消"/"确认切换"<br>- 若用户选了类型标签或填了描述，追加提示"同时上报你选择的问题反馈"<br><br>**交互：**<br>- 点击"取消" → 返回弹窗主内容<br>- 点击"确认切换" → 弹窗关闭 → 展示成功状态|
|应用成功|demo成功态|**展示：** 蒙层 + 居中展示：✅图标 + "应用成功" + "请重新启动游戏"<br><br>**交互：** 展示2.6秒后自动消失，或用户点击任意位置关闭|
|引擎方案数据|—|**数据来源：** 后台推荐配置表，按芯片+游戏匹配<br>- 每个方案：方案名（如"系统智能设置方案"）+ 启动成功率（如94.4%）+ 推荐标签（可选）<br>- 最多展示3个方案，按成功率降序排列<br>- 第1个方案默认选中|
|问题反馈提交|—|**提交内容：**<br>- 用户选择的异常类型标签（可多选）<br>- 用户输入的描述文字（可选，最多200字）<br>- 自动附带：设备型号、芯片、APP版本、OS版本、内存、当前引擎配置、游戏ID<br><br>**提交时机：** 用户点击"确认切换"时一并提交|

#### 4.2.1 边界条件-内容侧

|场景|处理方式|
|---|---|
|推荐配置表中无该芯片数据|展示通用全局方案（全芯片成功率最高的3个方案）|
|推荐配置表中无该游戏数据|展示该芯片维度全局成功率最高的3个方案|
|方案不足3个|有几个展示几个，至少展示1个|
|所有方案成功率相同|按方案创建时间倒序展示|
|异常类型标签被后台全部禁用|隐藏整个"启动异常类型"区域|
|网络异常无法获取方案数据|使用本地缓存的上次方案数据；若无缓存则弹窗不弹出|

#### 4.2.2 边界条件-用户侧

|场景|处理方式|
|---|---|
|用户点击"暂不处理"关闭弹窗|本次不再弹出，下次启动该游戏若仍异常则再次弹出|
|同一游戏多次触发|24小时内最多弹出2次|
|用户未选方案直接点"切换方案"|默认选中第1个方案，可直接提交|
|描述输入超过200字|禁止继续输入，显示字数统计"200/200"|
|弱网下提交反馈|本地缓存反馈数据，恢复网络后自动上报|
|切换方案后仍异常|下次弹出时不做特殊处理，同正常流程|

### 4.3 详细设计（B端）

功能demo：`demos/兼容性诊断后台demo.html`

|模块名称|图示|展示&交互说明|
|---|---|---|
|问题上报列表|demo上报模块|**展示：**<br>- 顶部4张统计卡片：今日上报（较昨日↑↓%）、本周上报（较上周%）、待处理（平均处理时长）、已解决（解决率%）<br>- 筛选区：异常类型下拉（全部/闪退/黑屏/花屏/卡顿/卡加载）、反馈类型下拉（全部/黑屏/启动报错/卡在启动加载/其他）、状态下拉（全部/待处理/处理中/已解决）<br>- 功能按钮："导出CSV"（ghost按钮）、"批量处理"（主按钮）<br>- 表格字段：时间、游戏、异常类型(badge)、反馈类型(badge)、设备、系统、APP版本、芯片、状态(badge)、操作("查看"按钮)<br>- 默认排序：按上报时间倒序<br>- 分页：每页20条，底部翻页器<br>- 空状态："暂无上报记录"<br><br>**交互：**<br>- 修改筛选 → 列表实时刷新<br>- 点击"导出CSV" → 导出当前筛选结果，toast"已导出"<br>- 点击"批量处理" → 需先勾选记录，批量标记状态<br>- 点击"查看" → 弹出详情弹窗|
|上报详情弹窗|demo详情|**展示：**<br>- 弹窗标题"上报详情"，右上角✕关闭<br>- 2列网格展示：游戏、异常类型(badge)、反馈类型(badge)、设备型号、芯片、系统（如MIUI 15）、APP版本、OS版本（如Android 14）、内存（总量+可用）、Wine/Proton版本、GPU驱动版本、DXVK版本<br>- 用户反馈原文：带引号展示在灰底框中<br>- 上报时间<br>- 底部按钮：左"关闭"(ghost)，右"标记已解决"(主按钮)<br><br>**交互：**<br>- 点击"标记已解决" → 状态变为绿色"已解决"，弹窗关闭，toast"已标记为已解决"|
|弹窗文案配置|demo文案配置模块|**展示：**<br>- 国内版/海外版Tab切换<br>- 表格"启动异常类型标签"：序号、标签名称（中文，可编辑input）、English（可编辑input）、标签KEY（不可编辑tag）、状态(badge启用/禁用)、操作(禁用/删除按钮)<br>- 右上角"+ 新增类型"按钮<br>- 顶部"保存配置"主按钮<br><br>**交互：**<br>- 直接在表格内编辑标签名称 → 点击"保存配置"生效<br>- 点击"+ 新增类型" → 弹出表单弹窗（标签名称中文、English、标签KEY 3个字段），保存后追加到列表<br>- 点击"禁用" → 该标签不在客户端展示，badge变红色"禁用"<br>- 点击"删除" → 二次确认弹窗，确认后从列表移除<br>- 切换国内/海外Tab → 展示对应区服的配置（独立维护）|

#### 4.3.1 边界条件-内容侧

|场景|处理方式|
|---|---|
|标签KEY重复|保存时提示"KEY已存在，请修改"，阻止保存|
|标签名称为空|保存时提示"请填写标签名称"，阻止保存|
|所有标签被禁用|客户端隐藏整个异常类型区域，不影响引擎方案切换主流程|
|删除已有上报记录关联的标签|历史记录保留原标签名称展示，不受影响|

#### 4.3.2 边界条件-用户侧

|场景|处理方式|
|---|---|
|运营同时编辑同一标签|后保存者覆盖，不做冲突检测（低频操作）|
|国内版和海外版标签不一致|各自独立维护，互不影响|

## 五、非功能需求

|需求类型|详细要求|
|---|---|
|性能|弹窗渲染 ≤ 200ms；方案数据接口响应 ≤ 500ms|
|兼容性|Android 8.0+；iOS 14+；支持骁龙8系列、天玑9000系列主流芯片|
|安全|上报数据不包含用户账号/密码；描述文字需敏感词过滤（提交时）|
|容错|方案接口超时降级为本地缓存；反馈提交失败本地暂存，恢复网络后重试|
|频控|同一游戏24小时内最多自动弹出2次|

## 六、埋点需求

### 6.1 埋点事件表

|事件ID|事件名称|触发时机|关键参数|
|---|---|---|---|
|diag_popup_show|弹窗展示|弹窗展示时|trigger_scene, game_id, chip, layout_mode|
|diag_engine_select|选择引擎方案|用户点击方案卡片|plan_name, plan_rate|
|diag_issue_select|选择异常类型|用户点击类型标签|issue_tags|
|diag_confirm|确认切换|用户点击"切换方案"|plan_name, issue_tags, has_feedback_text|
|diag_confirm_final|最终确认|用户点击"确认切换"|plan_name|
|diag_dismiss|关闭弹窗|用户点击"暂不处理"或✕|trigger_scene, game_id|
|diag_success|应用成功|成功状态展示时|plan_name, game_id|

### 6.2 埋点参数表

|参数名|类型|必填|说明|枚举/示例|
|---|---|---|---|---|
|trigger_scene|string|是|触发场景|black_screen / crash / stuck_loading|
|game_id|string|是|游戏唯一标识|#1245620|
|chip|string|是|设备芯片型号|snapdragon_8_gen3|
|layout_mode|string|是|弹窗形态|landscape / portrait|
|plan_name|string|是|选择的方案名|系统智能设置方案|
|plan_rate|string|是|方案成功率|94.4%|
|issue_tags|string[]|否|选择的异常类型标签|["black_screen","stuck_loading"]|
|has_feedback_text|boolean|否|是否填写了描述|true / false|

## 七、运营需求

1. **引擎方案维护：** 按芯片+游戏维度持续更新推荐引擎方案数据及启动成功率
2. **异常类型标签配置：** 上线前完成国内版（中文）和海外版（中+英）的标签配置
3. **上报问题跟进：** 每日处理待处理上报，根据反馈类型分布调整引擎方案推荐优先级
4. **数据分析：** 定期分析"切换方案后仍失败"的case，推动引擎侧优化

## 八、国内海外差异

|差异项|国内版（盖世游戏）|海外版（GameHub）|
|---|---|---|
|弹窗语言|中文|跟随用户语言设置（中/英/日/俄/葡）|
|异常类型标签|后台国内版配置，仅中文|后台海外版配置，中文+English|
|问题上报|上报至国内运营后台|上报至海外运营后台|
|引擎方案数据源|国内推荐配置表|海外推荐配置表（芯片覆盖范围不同）|
|APP名称|盖世游戏|**GameHub**（禁止用GaishiGame）|

## 九、来自功能上线后的更新

（上线后记录）
