# 【Prd】《盖世游戏》GOG平台接入需求

## 一、版本信息

|时间|版本|变更人|主要变更内容|备注|
|---|---|---|---|---|
|2026.08.11|V1.0|郑群超|新增 GOG 账号绑定、游戏库、详情启动、平台切换及横竖屏搜索需求|仅 C 端；覆盖国内包与海外包|
|2026.08.11|V1.1|郑群超|按现行横竖页面返修为 10 个视图，采用新版游戏库结构并删除 GOG 账号价值展示|入口顺序固定为 EPIC → GOG → 导入游戏|
|2026.08.13|V1.2|郑群超|同步最终账号菜单、EPIC“喜加一”、多平台搜索分条、详情启动 icon、“获得游戏”标识及游戏归类口径|与最终交互标注 Demo 一致|

## 二、背景与目标

### 2.1 需求背景

拥有 GOG 游戏的用户目前无法在盖世游戏内绑定 GOG、查看个人游戏库、识别 GOG 搜索结果，也无法从游戏详情明确选择 GOG 启动。用户只能离开应用，通过 GOG Galaxy 或 GOG 网站查看账号和游戏资产，盖世游戏内的发现、查看、启动链路不完整。

现有产品已在我的页、游戏库、游戏详情和搜索结果中接入 Steam、EPIC。本期先按现行横竖版页面和新版游戏库还原既有结构，再以最小增量增加 GOG，不新增平台管理中心，不改变 Steam、EPIC 既有核心流程。

需求证据与判断：

|类型|当前证据|结论|
|---|---|---|
|业务输入|任务明确要求接入 GOG，并覆盖我的页、游戏库、游戏详情和横竖屏搜索结果|范围已确认|
|现有产品|现行页面已具备 EPIC 账号入口、账号游戏库、详情启动和搜索平台标识；新版游戏库已明确 GOG 入口空位|GOG 复用 EPIC 信息层级，入口固定放在 EPIC 与导入游戏之间|
|用户替代方案|用户需通过 GOG Galaxy 或 GOG 网站查看 GOG 资产|应用内链路存在缺口|
|用户研究与行为数据|当前没有 GOG 用户规模、绑定意愿、有效库覆盖率和启动增量基线|[高风险假设] 用户存在足够的 GOG 资产管理和启动需求；上线前后必须通过埋点验证|
|外部能力|尚未完成 GOG 官方授权方式、游戏库字段、令牌刷新和退出能力验证|[高风险假设] 正式开发以官方可用能力为前提，禁止用采集用户密码替代|

### 2.2 目标与成功指标

|目标类型|目标|指标口径|基线|目标值|观察周期|
|---|---|---|---|---|---|
|链路完整|用户可从我的页或游戏库完成 GOG 绑定，并查看 GOG 游戏|GOG 绑定成功次数 / GOG 授权提交次数|[待确认] 上线前无基线|[高风险假设] 默认建议首个完整灰度周期不低于 80%|灰度开始后 14 天|
|数据可用|绑定成功后可完成首次游戏库同步|首次同步成功次数 / GOG 绑定成功次数|[待确认] 上线前无基线|[高风险假设] 默认建议不低于 90%|灰度开始后 14 天|
|映射可靠|GOG 返回游戏可正确归入统一游戏|成功映射到统一 `gameId` 的 GOG 游戏数 / GOG 返回游戏数|[待确认] 需以接口样本建立|[高风险假设] 默认建议高频游戏抽样准确率不低于 99%，低置信度不计为已映射|灰度前抽样及灰度后 14 天|
|启动可用|用户从 GOG 来源进入详情后可按 GOG 上下文启动|GOG 启动成功次数 / GOG 启动请求次数|[待确认] 上线前无基线|[高风险假设] 默认建议不低于现有 EPIC 同口径|灰度开始后 14 天|
|异常可恢复|授权失效、请求失败和缓存降级后可重试或返回|恢复成功次数 / 恢复操作次数，按失败类型拆分|[待确认] 上线前无基线|[高风险假设] 默认建议核心失败类型均可恢复且无账号串改|提测及灰度全周期|
|业务验证|验证 GOG 接入是否带来有效启动增量|灰度用户人均 GOG 启动次数与上线前同周期对比|[待确认] 需先建立同口径基线|[待确认] 默认建议先观察，不在缺少基线时设强制增长值|灰度前后各 14 天|

指标目标缺少用户与历史数据支撑，均不作为既成事实。最终阈值见 9.2 待确认项。

### 2.3 范围与不做事项

|类型|内容|
|---|---|
|本期范围|仅 C 端；国内包与海外包；我的页；GOG 官方授权；新版游戏库首页横竖版；GOG 账号游戏库横竖版；搜索结果横竖版；游戏详情横竖版；启动平台切换；账号与异常恢复；埋点与安全约束；保留三栏交互标注壳|
|不做事项|不新增全平台管理中心；不新增 B 端后台；不改造 Steam、EPIC 核心流程；不合并搜索中的多平台结果；不新增 GOG 评分口径；不增加与 GOG 接入无关的业务、页面、弹窗、Toast 或流程|
|硬性约束|先还原现行页面再增加 GOG；游戏库入口顺序为 EPIC → GOG → 导入游戏；明确入口携带 `sourcePlatform`；来源不可用时不静默回退；无来源时才按 Steam > EPIC > GOG 选择；游戏归类使用 `gameId` 与 `platformAppId`；名称仅作候选匹配；低置信度不合并；只在 GOG 官方页面输入凭证；盖世游戏不保存 GOG 邮箱或密码|
|地区与包体|国内包 APP 名为“盖世游戏”，固定中文且不可切换语言；海外包 APP 名为“GameHub”，支持跟随系统、中文、英语、日语、俄语、巴西葡萄牙语；GOG 功能、状态和路由规则一致|

## 三、故事介绍

### 3.1 用户与运营场景

- GOG 资产用户：在我的页或新版游戏库首页看到 GOG 入口，点击后进入 GOG 官方网页。授权成功并完成首次同步后，进入 GOG 账号游戏库，查看头像、用户名、GOG ID、游戏数量、总游玩时长和游戏列表。
- GOG 游戏用户：在新版游戏库首页点击 GOG，进入已同步的 GOG 账号游戏库。点击游戏后进入详情，页面保留 GOG 来源并展示 GOG 启动方式。
- 多平台用户：从某个平台的游戏库或搜索结果进入详情时，默认使用该来源平台；从没有平台上下文的入口进入时，系统才在已拥有且账号可用的平台中按 Steam > EPIC > GOG 选择。
- 搜索用户：在竖屏或横屏搜索结果中看到 EPIC、GOG 平台标识。同一个 PC 游戏存在多个平台时，每个平台版本展示为独立结果，点击后进入对应平台详情。
- 运营与产品：无需新增 B 端配置，通过既有版本开关、语言资源和监控体系上线；上线后观察绑定、同步、映射和启动指标。

### 3.2 价值分析

- 用户价值：减少用户在盖世游戏与 GOG Galaxy、GOG 网站之间往返的操作，在同一应用内完成资产查看和启动。
- 产品价值：补齐 Steam、EPIC、GOG 三个平台的核心消费链路，提高个人游戏资产覆盖范围。
- 风险与取舍：复用现行 EPIC 入口和账号库结构可控制学习与开发成本，但继续增加平台时会增加游戏库入口空间压力。本期按新版游戏库既定空位增加 GOG，不提前建设平台管理中心。
- [高风险假设] 当前没有用户覆盖和启动增量数据，商业价值只能通过上线后的绑定、同步、映射与启动数据验证。

### 3.3 核心体验路径

1. 首次绑定：我的页或新版游戏库首页 GOG 入口 → 点击“绑定 GOG 账号” → 打开 GOG 官方网页 → 用户在官方页面完成登录授权 → 首次同步 → 进入或返回 GOG 账号游戏库。
2. 游戏库启动：新版游戏库首页 → 按 EPIC → GOG → 导入游戏 的固定顺序点击 GOG → 进入 GOG 账号游戏库 → 点击游戏 → 携带 `sourcePlatform=gog` 与对应 `platformAppId` 进入详情 → 展示 GOG 上下文 → 点击 GOG 启动。
3. 搜索启动：探索页搜索 → 竖屏或横屏搜索结果 → 点击 GOG 结果 → 携带 GOG 来源进入详情 → 展示 GOG 启动。
4. 平台切换：游戏详情 → 点击详情卡片内的平台 tab → 打开切换启动平台弹窗 → 选择已拥有的平台 → 同步切换时长、云存档、平台标识和启动 icon；点击弹窗外部或取消则保持原平台。
5. 账号维护：我的页已绑定账号卡 → 点击右上角 `…` → 选择更新数据、切换账号或退出账号 → 只处理当前平台状态，不改变其他平台。

### 3.4 产品指标预测

|指标|当前判断|使用方式|
|---|---|---|
|GOG 绑定点击率|[高风险假设] 无基线，不预测绝对值|判断入口是否被目标用户发现|
|GOG 绑定成功率|[高风险假设] 默认建议不低于 80%|定位官方授权或网络阻塞|
|首次同步成功率|[高风险假设] 默认建议不低于 90%|判断接口与数据处理稳定性|
|有效库覆盖率|[高风险假设] 取决于 GOG 字段及游戏映射库|识别映射缺口，不以名称强行合并|
|GOG 启动成功率|[待确认] 默认与现有 EPIC 同口径比较|判断 GOG 启动能力是否可上线|
|GOG 启动增量|[待确认] 先建立上线前同周期基线|验证是否改变用户启动行为|

### 3.5 路径规划

|阶段|范围|进入下一阶段的条件|
|---|---|---|
|技术验证|验证 GOG 官方授权、令牌生命周期、游戏库字段和退出机制|不采集用户密码即可完成授权；关键字段与失败码可识别|
|内部验收|完成 10 个页面视图、账号隔离、来源路由、异常恢复和安全检查|9.1 P0 用例全部通过；无敏感凭证进入日志、埋点或本地存储|
|灰度验证|国内包与海外包按既有灰度能力开放|绑定、首次同步、启动及映射抽样达到确认后的阈值|
|全量开放|两包功能规则一致，多语言与监控就绪|无 P0/P1 缺陷，主要指标无异常，具备关闭开关与回退方案|

具体灰度比例和时间点为 [待确认]，默认建议从内部账号和小比例用户开始，见 9.2。

## 四、概要设计

### 4.1 模块设计

|模块|目标用户|核心能力|依赖|优先级|
|---|---|---|---|---|
|我的页平台入口|需要绑定和管理第三方平台的用户|未绑定引导、绑定状态、`…` 账号菜单、当前平台更新/切换/退出及 EPIC“喜加一”|既有第三方账号框架、GOG 授权与账号接口|P0|
|GOG 官方登录|首次绑定、重新登录和切换账号用户|在 GOG 官方页面完成凭证输入和授权|受信任域名 WebView、授权回调、令牌体系|P0|
|新版游戏库首页|进入个人游戏资产的用户|横竖屏按现行页面展示 EPIC → GOG → 导入游戏|现行游戏库结构、平台绑定状态|P0|
|GOG 账号游戏库|已绑定 GOG 的用户|按 EPIC 账号库层级展示账号摘要、GOG 游戏、空态、缓存和重试|GOG 游戏库接口、游戏映射服务|P0|
|游戏详情与启动|从 GOG 或多平台入口进入的用户|展示平台数据、保留来源、选择启动平台|详情聚合、平台账号状态、启动能力|P0|
|横竖屏搜索结果|搜索 PC 游戏的用户|展示 EPIC、GOG 分条结果并传递平台上下文|搜索服务、游戏映射服务|P0|
|状态与数据层|全部平台用户|账号隔离、来源路由、缓存恢复、低置信度不合并|既有状态管理与缓存体系|P0|
|埋点与监控|产品、研发、测试|记录曝光、授权、同步、映射、启动和失败类型|既有埋点与监控体系|P0|

### 4.2 详细设计（C端）

本 PRD 无图示；交互与页面状态以同目录交付的单文件标注 Demo 为准。

Demo 共 10 个页面视图：我的页、GOG 授权、游戏库首页横竖版、GOG 账号库横竖版、搜索横竖版、详情横竖版。未绑定、loading、empty、error、expired、cancelled、cached 作为对应视图的可切换状态，不重复建立页面导航项。

Demo 保留三栏标注壳：左侧为页面、方向和状态入口；中间为可操作的现行 APP 页面；右侧为交互说明与异常边界。GOG 增量必须建立在对应横竖页面还原完成之后。

GOG 不展示账号价值或“喜加一”；页面不渲染对应标题、数值、按钮、骨架或占位空间，也不以 0 或“--”代替，账号卡按实际内容自然收缩。仅 EPIC 账号卡在原三项账号操作所在位置展示一个“喜加一”按钮。

|模块名称|图示|展示&交互说明|
|---|---|---|
|① 我的页|无图，以标注 Demo 为准|**基线**：按现行我的页竖屏结构还原，在 Steam、EPIC 既有平台区域增加 GOG，不移动无关区块。<br>**未绑定**：沿用 EPIC 的未绑定表现；点击“绑定 GOG 账号”进入官方授权。<br>**已绑定**：显示头像、用户名、GOG ID、游戏数、总时长和最近同步时间；GOG 不显示账号价值或“喜加一”，不保留空位，账号卡按实际内容自然收缩。<br>**账号菜单**：已绑定账号卡右上角显示 `…`；点击后展开“更新数据 / 切换账号 / 退出账号”，三项操作只在菜单中展示；点击菜单外部关闭。更新进行中禁用重复提交；切换取消、授权失败或首次同步失败时保留旧账号；退出需二次确认，确认后只清理当前平台的令牌与账号缓存。<br>**平台专属操作**：仅 EPIC 账号卡在原三项操作所在位置显示一个“喜加一”按钮；Steam、GOG 不显示该按钮。<br>**隔离**：更新、切换、退出、expired 和 cancelled 均不改变其他平台。|
|② GOG 授权|无图，以标注 Demo 为准|**入口**：我的页、新版游戏库首页，或 expired 状态下的重新登录。<br>**页面**：内置 WebView 加载 GOG 官方登录页；邮箱、密码和“记住我”由 GOG 官方页面处理；应用侧不创建本地凭证输入框。<br>**交互**：授权成功 → 接收授权结果 → 首次同步 → 回到发起入口或进入 GOG 账号游戏库；cancelled、拒绝、网络失败、超时 → 回到原入口并保持原账号。<br>**安全**：盖世游戏/GameHub 不保存 GOG 邮箱或密码，不通过本地存储写凭证；超出允许名单的跳转中止授权。|
|③ 游戏库首页：竖屏|无图，以标注 Demo 为准|**基线**：按用户确认的新版游戏库竖屏结构还原。<br>**入口顺序**：GOG 固定插入既定空位，顺序为 EPIC → GOG → 导入游戏；其他入口的名称、顺序和交互不变。<br>**GOG 入口**：视觉与点击区域沿用相邻 EPIC 入口。<br>**交互**：未绑定 → GOG 授权；已绑定 → GOG 账号游戏库：竖屏。<br>**验收**：新增 GOG 后不挤压、覆盖或重排无关入口。|
|④ 游戏库首页：横屏|无图，以标注 Demo 为准|**基线**：按用户确认的新版游戏库横屏结构独立还原，不通过旋转或整体缩放生成。<br>**入口顺序**：与竖屏一致，为 EPIC → GOG → 导入游戏。<br>**交互**：未绑定进入 GOG 授权；已绑定进入 GOG 账号游戏库：横屏。<br>**方向切换**：保留 GOG 绑定状态、当前入口和滚动位置。|
|⑤ GOG 账号游戏库：竖屏|无图，以标注 Demo 为准|**基线**：参照现行 EPIC 账号库竖屏结构，平台内容替换为 GOG。<br>**账号区**：保留头像、用户名、GOG ID、游戏数和总时长；剩余内容按 EPIC 原有对齐规则自然重排。<br>**游戏列表**：沿用 EPIC 的封面、标题、GOG 标识、游玩时长、排序、滚动和返回位置。<br>**交互**：点击游戏携带 `gameId`、`platformAppId`、`sourcePlatform=gog` 进入游戏详情：竖屏。<br>**状态**：empty 显示“暂无 GOG 游戏”；error 无缓存时显示失败和重试；cached 展示缓存时间和重试；expired 引导重新登录。|
|⑥ GOG 账号游戏库：横屏|无图，以标注 Demo 为准|**基线**：参照现行 EPIC 账号库横屏结构独立还原，平台内容替换为 GOG。<br>**账号与列表**：字段、排序、滚动和状态规则与竖屏共用，只改变排布。<br>**交互**：点击游戏携带相同 `gameId`、`platformAppId`、`sourcePlatform=gog` 进入游戏详情：横屏。<br>**方向切换**：保留账号、列表状态、滚动位置和失败恢复上下文。|
|⑦ 搜索结果：竖屏|无图，以标注 Demo 为准|**基线**：按现行竖屏搜索页面还原，再增加 GOG 结果。<br>**展示**：新增 EPIC 和 GOG 平台标识；同一个 PC 游戏存在多个平台时，每个平台版本展示为独立结果，不按名称强制合并；每条结果关联 `gameId`、平台和 `platformAppId`。<br>**交互**：点击 GOG 结果传递 `sourcePlatform=gog`，进入游戏详情：竖屏。<br>**评分**：EPIC 原始 5 分制按“原始分数 × 2”换算到 10 分制并限制在 0 至 10；GOG 没有可用评分时显示“暂无评分”，不复用 EPIC 评分。<br>**状态**：loading、empty、error 沿用现行搜索规则，空结果与请求失败分开。|
|⑧ 搜索结果：横屏|无图，以标注 Demo 为准|**基线**：按现行横屏搜索页面独立还原，再增加 GOG 结果。<br>**规则**：与竖屏共用搜索数据、分条规则、评分规则和点击处理，只改变列表排布。<br>**交互**：点击 GOG 结果携带相同 `sourcePlatform`、`gameId`、`platformAppId` 进入游戏详情：横屏。<br>**方向切换**：保留搜索词、滚动位置、平台上下文和账号状态。|
|⑨ 游戏详情：竖屏|无图，以标注 Demo 为准|**基线**：按现行竖屏详情页还原，再增加 GOG 数据和启动入口；“获得游戏”区域同步增加 Steam、EPIC、GOG 平台标识。<br>**来源**：从 GOG 账号游戏库或搜索进入时，`sourcePlatform=gog`，`selectedPlatform=gog`，展示 GOG 标识、游戏时长、云存档状态和 GOG 启动 icon；点击启动 icon 按当前平台参数启动游戏。<br>**平台切换**：点击详情卡片内的平台 tab 打开切换平台弹窗；选择平台后同步更新平台标识、游戏时长、云存档和启动 icon；点击弹窗外部或取消保持原平台。<br>**语义**：“获得游戏”区域的平台标识仅表示可获取渠道，不等同于详情启动按钮；启动入口以详情卡片当前平台的启动 icon 为准。<br>**异常**：GOG 来源不可用时提示重新登录或手动切换，不静默改用 Steam、EPIC；云存档字段不可用显示“未获取”；启动失败时提示失败并保留当前详情数据与平台选择，不清空或跳离详情。|
|⑩ 游戏详情：横屏|无图，以标注 Demo 为准|**基线**：按现行横屏详情页独立还原，再增加 GOG 数据、启动 icon 及“获得游戏”区域的 Steam、EPIC、GOG 标识。<br>**规则**：来源、选中平台、游戏时长、云存档、平台切换弹窗、启动、“获得游戏”标识语义和异常规则与竖屏共用，只改变排布。<br>**无来源默认**：仅入口没有 `sourcePlatform` 时，在已拥有且账号可用的平台中按 Steam > EPIC > GOG 选择。<br>**方向切换**：保留 `sourcePlatform`、`selectedPlatform`、账号状态和平台切换结果。|

#### 4.2.10 全局平台路由与游戏归类

平台路由：

1. 入口有明确 `sourcePlatform` 且用户拥有、账号可用：`selectedPlatform` 使用来源平台。
2. 入口有明确 `sourcePlatform`，但账号未绑定、授权失效或平台不可用：保留来源平台，展示绑定、重新登录或切换平台；不得自动替换。
3. 入口没有 `sourcePlatform`：在用户已拥有且账号已绑定、令牌有效的平台中按 Steam > EPIC > GOG 选择。
4. 用户主动切换后，`selectedPlatform` 使用用户选择，直到离开详情或再次切换。

游戏归类：

1. 统一 `gameId` 表示盖世游戏内同一个游戏。
2. `platform + platformAppId` 唯一定位一个平台版本；`platformAppId` 不跨平台复用。
3. 英文名称、中文名称、别名和发行版本只用于生成候选匹配，不直接决定归类。
4. 候选无法可靠归类或置信度低时，按不同详情处理，不合并；经人工映射或服务端确认后，才能调整统一 `gameId`。
5. 搜索结果始终按平台版本分条展示；游戏详情可基于统一 `gameId` 展示用户拥有的平台。

#### 4.2.11 用户故事与前后条件

- 用户故事：作为拥有 GOG 游戏的用户，我希望绑定 GOG 并在盖世游戏或 GameHub 中查看、搜索和启动自己的 GOG 游戏，以便减少跨应用查找与启动。
- 前置条件：用户已进入既有账号体系；客户端版本支持 GOG；GOG 官方授权和对应接口可用；入口能传递平台上下文。
- 成功后置条件：保存授权结果及按既有安全体系保存的令牌；完成当前 GOG 账号首次同步；更新 GOG 账号、游戏库和详情数据；不改变 Steam、EPIC。
- 失败后置条件：失败、取消、超时不替换旧账号；无旧账号时维持未绑定；来源平台上下文不丢失；用户可重试或返回。

#### 4.2.12 状态流转

|当前状态|触发事件|条件|下一状态|失败处理|
|---|---|---|---|---|
|unbound|点击绑定|GOG 官方页可达|authorizing|进入 error，提供重试和返回|
|authorizing|授权成功|回调合法|syncing|授权拒绝或 cancelled 时返回原入口，原状态不变|
|syncing|首次同步成功|账号和基础库数据可用|bound|有旧账号时保留旧账号；无旧账号时回到 unbound 或 error|
|bound|点击更新刷新|未有进行中的刷新|loading 后回到 bound|保留旧数据，显示 error；有缓存时进入 cached 表现|
|bound|点击切换账号|新账号未完成授权和同步|authorizing|cancelled 或 error 时保留旧账号|
|bound|确认退出|用户二次确认|unbound|清理失败时不得伪装退出成功，记录非敏感错误并重试|
|bound|令牌失效|接口返回授权失效|expired|重新登录成功后回到原入口；Steam、EPIC 不变|
|error|点击重试|接口恢复|loading 后进入目标状态|继续保留重试和返回|
|cached|点击重试|存在上次缓存|成功进入 bound 并更新时间|继续展示缓存和时间，不清空可用内容|
|任一登录入口|用户取消|未产生有效新授权|cancelled 后返回原入口|恢复进入前状态和滚动位置|

六类可模拟恢复状态定义：

|状态值|触发条件|页面表现|允许操作|恢复要求|
|---|---|---|---|---|
|loading|正在授权、同步、刷新或拉取列表|骨架或加载提示，提交按钮禁用|返回；不可重复提交|成功进入内容，失败进入 error、expired 或 cached|
|empty|已绑定且接口成功返回 0 个游戏|空数据说明|刷新、返回|有数据后展示列表|
|error|请求失败且没有可用缓存|错误说明|重试、返回|成功后进入目标内容|
|expired|令牌过期或被撤销|重新登录说明|重新登录、返回|成功后回到原入口，不改其他平台|
|cancelled|用户取消官方登录或账号切换|返回原入口|再次绑定或切换|恢复原账号与原页面状态|
|cached|请求失败且有上次可用缓存|缓存内容、缓存时间、重试入口|重试、返回|成功后替换缓存并更新时间|

#### 4.2.13 数据字典

|字段名|类型|必填|业务含义|校验规则|示例值|
|---|---|---|---|---|---|
|`gameId`|string|是|盖世游戏统一游戏标识|同一游戏跨平台一致；只由已确认映射生成|cyberpunk-2077|
|`platform`|enum|是|平台类型|steam、epic、gog|gog|
|`platformAppId`|string|是|平台侧游戏标识|与 platform 组合后唯一|gog-1423049311|
|`sourcePlatform`|enum 或 null|是|进入详情时的来源平台|明确平台入口必须传；无平台入口传 null|gog|
|`selectedPlatform`|enum 或 null|是|详情当前展示与启动的平台|来源优先；无来源才按平台优先级计算|gog|
|`ownedPlatforms`|enum 数组|是|用户已拥有并经校验的平台集合|去重；不得仅按游戏名称推断|steam、epic、gog|
|`bindStatus`|enum|是|单个平台账号绑定状态|unbound、authorizing、syncing、bound、expired、error|bound|
|`tokenStatus`|enum|是|授权令牌可用状态|none、valid、expired；不得记录令牌原文|valid|
|`mappingConfidence`|enum|是|平台游戏映射置信度|confirmed、high、low；low 不自动合并|confirmed|
|`requestState`|enum|是|页面请求与恢复状态|normal、loading、empty、error、expired、cancelled、cached|cached|
|`rawScore`|number 或 null|否|平台原始评分|EPIC 取 0 至 5；GOG 未确认口径时为 null|4.4|

#### 4.2.14 系统集成与降级说明

|系统|方向|数据或接口|触发方式|失败降级|责任方|
|---|---|---|---|---|---|
|GOG 官方授权|客户端 ↔ GOG|官方登录、授权结果、令牌刷新、撤销|绑定、重新登录、切换、退出|页面不可达或授权失败时重试、返回；禁止改为本地凭证输入|客户端、服务端、GOG|
|GOG 账号游戏库|服务端 ↔ GOG|头像、用户名、GOG ID、游戏数、总时长、游戏列表、单游戏时长及可用字段|首次同步、刷新、定时同步|有缓存展示 cached；无缓存展示 error；未获官方支持的字段不渲染虚假数据|服务端、GOG|
|游戏映射服务|服务端内部|`gameId`、平台、`platformAppId`、映射置信度|同步与搜索入库|低置信度不合并；保留独立平台版本并进入抽样校验|服务端、产品|
|详情与启动服务|客户端 ↔ 服务端|拥有平台、账号状态、启动参数|进入详情、切换平台、点击启动|来源不可用时保留来源并引导用户选择，不静默回退|客户端、服务端|
|搜索服务|客户端 ↔ 服务端|多平台搜索结果与评分|竖屏、横屏搜索|平台字段缺失的结果不得伪装为 GOG；GOG 没有可用评分时显示“暂无评分”|客户端、服务端|
|埋点与监控|客户端、服务端 → 数据平台|事件、非敏感参数、失败类型|曝光、点击、结果、异常|上报失败不得阻塞核心链路；禁止包含账号凭证和令牌|客户端、服务端、数据|

## 五、非功能需求

|需求类型|详细要求|验收方式|
|---|---|---|
|性能|账号卡、游戏库和搜索结果的性能口径沿用同版本 Steam、EPIC；新增 GOG 不得使现有页面出现可感知阻塞；授权、同步和启动耗时需分阶段记录|与同版本 Steam、EPIC 在相同网络和数据量下对比；记录 P50、P95，并在技术验证后确认告警阈值|
|兼容性|覆盖当前版本支持的国内包、海外包、竖屏、横屏和最小支持宽度；横竖屏切换保留账号、来源平台和选中平台|真机与自动化覆盖两包、两种方向和最小支持宽度；无标识截断、区域重叠和状态丢失|
|安全与合规|仅在 GOG 官方页面输入凭证；客户端不读取、不保存、不上报邮箱和密码；不通过本地存储写凭证；授权 token 仅按既有安全体系加密保存；日志、埋点、错误上报和 WebView 截图不得包含登录内容；域名使用允许名单|代码扫描、存储检查、日志与抓包检查、域名跳转测试、退出清理测试|
|容错与降级|完整支持 loading、empty、error、expired、cancelled、cached；请求失败优先保留合法缓存；来源不可用不静默切换；重复提交按钮禁用|按 4.2.12 注入故障，逐项验证页面表现、操作和恢复结果|
|数据生命周期|令牌按既有第三方平台规范保存和过期清理；退出清除当前 GOG 令牌与账号级缓存；切换账号在新账号同步完成后替换旧账号；Steam、EPIC 数据不受影响|退出、切换、过期、应用重启及并发刷新测试；检查三个平台数据快照|
|一致性|同一平台操作只更新当前平台；横竖屏、我的页、游戏库、详情和搜索使用同一账号与路由状态|跨页面和横竖屏切换测试；比较账号、`sourcePlatform`、`selectedPlatform`|
|可观测性|授权、首次同步、游戏映射、缓存、启动各阶段均记录成功、失败和失败类型；不得记录第三方账号标识原文、邮箱、密码或令牌|检查事件样本、服务日志和监控看板；敏感信息扫描无命中|
|多语言|国内包固定中文且不可切换；海外包使用 GameHub 品牌名并支持既有多语言；GOG 官方网页语言由 GOG 决定|逐语言检查品牌名、功能文案、截断和回退语言；不出现 GaishiGame|

## 六、埋点需求

### 6.1 埋点事件表

|事件ID|事件名称|触发时机|关键参数|指标用途|
|---|---|---|---|---|
|`gog_entry_view`|GOG 入口曝光|我的页或游戏库 GOG 入口完成展示|`entry_page`、`orientation`、`app_package`、`bind_status`|绑定点击率分母|
|`gog_bind_click`|点击绑定 GOG|点击“绑定 GOG 账号”|`entry_page`、`orientation`、`app_package`|绑定意愿与漏斗|
|`gog_authorization_result`|GOG 授权结果|官方授权成功、失败、取消或超时|`entry_page`、`result`、`failure_type`、`duration_ms`|绑定成功率与失败归因|
|`gog_initial_sync_result`|首次同步结果|授权成功后的首次同步结束|`result`、`failure_type`、`duration_ms`、`game_count`|首次同步成功率|
|`gog_account_action`|GOG 账号操作|点击刷新、切换、退出及操作结束|`action`、`result`、`failure_type`、`request_state`|账号维护成功率|
|`gog_library_view`|GOG 账号游戏库曝光|GOG 账号游戏库展示完成|`request_state`、`game_count`、`cache_used`、`orientation`|有效使用和异常占比|
|`gog_game_click`|点击 GOG 游戏|点击 GOG 账号游戏库卡片|`game_id`、`platform_app_id`、`orientation`|账号游戏库到详情转化|
|`platform_detail_view`|平台详情曝光|详情平台内容展示完成|`game_id`、`source_platform`、`selected_platform`、`bind_status`|来源路由正确性|
|`platform_switch_result`|切换启动平台结果|用户选择平台或取消切换|`game_id`、`from_platform`、`selected_platform`、`result`、`failure_type`|多平台选择行为|
|`platform_launch_result`|平台启动结果|点击启动及启动结果返回|`game_id`、`platform_app_id`、`source_platform`、`selected_platform`、`result`、`failure_type`、`duration_ms`|GOG 启动成功率|
|`search_platform_result_click`|点击平台搜索结果|点击 EPIC 或 GOG 搜索结果|`game_id`、`platform_app_id`、`platform`、`orientation`、`score_shown`|搜索平台分布与详情转化|
|`gog_recovery_action`|异常恢复操作|在 error、expired、cached 状态点击重试、重新登录或返回|`entry_page`、`request_state`、`action`、`result`、`failure_type`|异常恢复成功率|

### 6.2 埋点参数表

|参数名|类型|必填|说明|枚举或示例|
|---|---|---|---|---|
|`entry_page`|string|是|入口页面|profile、library、detail、search|
|`orientation`|string|是|页面方向|portrait、landscape|
|`app_package`|string|是|包体|domestic、overseas|
|`app_language`|string|是|客户端当前语言|zh-CN、en、ja、ru、pt-BR|
|`game_id`|string|按事件|统一游戏标识|cyberpunk-2077|
|`platform_app_id`|string|按事件|平台游戏标识|gog-1423049311|
|`platform`|string|按事件|当前对象平台|steam、epic、gog|
|`source_platform`|string 或 null|按事件|详情来源平台|steam、epic、gog、null|
|`selected_platform`|string 或 null|按事件|详情选中平台|steam、epic、gog、null|
|`bind_status`|string|按事件|当前平台绑定状态|unbound、authorizing、syncing、bound、expired、error|
|`request_state`|string|按事件|页面请求与恢复状态|normal、loading、empty、error、expired、cancelled、cached|
|`result`|string|按事件|操作结果|success、failed、cancelled、timeout|
|`failure_type`|string|否|不含敏感信息的失败分类|network、authorization_denied、token_expired、sync_failed、mapping_failed、launch_failed|
|`action`|string|按事件|用户操作|bind、refresh、switch、logout、retry、relogin、back|
|`duration_ms`|integer|否|阶段耗时，毫秒|1830|
|`game_count`|integer|否|同步或展示的 GOG 游戏数|126|
|`cache_used`|boolean|否|本次是否展示缓存|true、false|
|`score_shown`|number 或 null|否|实际展示的 10 分制评分；无评分传 null|8.8|

埋点禁止参数：第三方账号标识原文、邮箱、密码、授权 token、Cookie、GOG 官方页面输入内容和页面截图。

### 6.3 指标计算与观察

|指标|计算方式|拆分维度|异常判断|
|---|---|---|---|
|GOG 绑定点击率|`gog_bind_click` 去重用户 / `gog_entry_view` 去重用户|入口、包体、方向、语言|仅观察；阈值见 9.2|
|GOG 绑定成功率|授权成功次数 / 授权提交次数|入口、包体、失败类型|连续低于确认阈值时暂停扩大灰度|
|首次同步成功率|首次同步成功次数 / 授权成功次数|包体、失败类型、耗时区间|连续低于确认阈值时暂停扩大灰度|
|有效库覆盖率|成功映射到统一 `gameId` 的 GOG 游戏数 / GOG 返回游戏数|游戏、地区、映射置信度|低置信度单列，不计为成功映射|
|GOG 启动成功率|GOG 启动成功次数 / GOG 启动请求次数|来源、包体、失败类型|与现有 EPIC 同口径比较|
|异常恢复成功率|恢复成功次数 / 恢复操作次数|requestState、入口、失败类型|账号串改计为 P0，不纳入普通失败率|

## 七、运营需求

本期不新增 B 端配置。运营、产品、研发和客服使用既有版本、语言、灰度和监控能力完成准备。

|优先级|准备事项|负责人角色|完成条件|最晚时间|
|---|---|---|---|---|
|P0|GOG 官方能力验证|服务端、客户端、安全|确认授权、回调、刷新、撤销、退出和允许域名；确认不采集用户密码|客户端开发前|
|P0|游戏字段与映射样本|服务端、产品、测试|取得真实字段样本；完成高频 GOG 游戏 `gameId`、`platformAppId` 抽样；低置信度不合并|联调前|
|P0|国内包与海外包文案|产品、本地化、测试|国内包固定中文；海外包 GameHub 品牌及既有多语言校对完成|提测前|
|P0|安全检查|安全、客户端、服务端|凭证、本地存储、日志、埋点、错误上报、WebView 历史和域名允许名单检查通过|灰度前|
|P0|监控与关闭开关|研发、数据、运维|授权、同步、映射、启动看板可用；具备停止新绑定和关闭 GOG 入口的既有开关|灰度前|
|P1|客服说明|产品、客服|覆盖官方登录失败、授权失效、游戏库为空、缓存数据、平台切换和退出影响范围|上线前|
|P1|灰度复盘|产品、数据、研发|按包体、入口和失败类型复盘指标，形成扩大或停止灰度结论|每个灰度阶段结束|

## 八、来自功能上线后的更新

本章节只记录真实上线后的规则、数据和问题变化。V1.0 编写时功能尚未上线，无上线后变更记录。后续每次更新需在版本信息中追加版本，并在本章记录日期、现象、证据、决策和影响范围，不回写为初始假设。

## 九、验收与待确认项

### 9.1 验收标准

|功能|前置条件|操作|预期结果|优先级|
|---|---|---|---|---|
|我的页未绑定|GOG 为 unbound|切换到 GOG 平台卡|展示绑定说明和绑定按钮；Steam、EPIC 状态不变|P0|
|GOG 官方登录|从我的页进入|点击绑定并完成官方授权|只在 GOG 官方页面输入凭证；成功后首次同步并返回我的页|P0|
|取消绑定|官方登录进行中|取消登录|返回原入口；requestState 经过 cancelled；原账号和其他平台不变|P0|
|我的页已绑定|GOG 为 bound|查看平台区域|展示头像、用户名、GOG ID、游戏数、总时长和同步时间；不渲染估值字段、分隔线、骨架或占位|P0|
|账号菜单|任一平台账号为 bound|点击账号卡右上角 `…`，再点击菜单外部|菜单只展示更新数据、切换账号、退出账号；三项不在卡片内平铺；点击外部后菜单关闭|P0|
|平台专属操作|Steam、EPIC、GOG 均有卡片|查看原三项操作所在位置|仅 EPIC 显示一个“喜加一”按钮；Steam、GOG 不显示；GOG 卡片不留空位并自然收缩|P0|
|更新数据|GOG 为 bound|从 `…` 菜单连续点击更新数据|首次点击后禁用重复提交；只更新 GOG；失败保留旧数据|P0|
|切换账号|已有 GOG 账号|授权新账号后取消或同步失败|继续展示旧账号；新账号完整同步前不得替换|P0|
|退出账号|三个平台均已绑定|从 `…` 菜单点击退出账号并二次确认|确认前不退出；确认后只清除当前 GOG token 和账号缓存；Steam、EPIC 仍为绑定且数据不变|P0|
|游戏库首页竖屏|竖屏且 GOG 为 unbound|查看入口并点击 GOG|入口顺序为 EPIC → GOG → 导入游戏；进入官方授权；无关入口不重排|P0|
|游戏库首页横屏|横屏且 GOG 为 bound|查看入口并点击 GOG|入口顺序与竖屏一致；进入 GOG 账号游戏库横屏；页面不是由竖屏旋转生成|P0|
|账号游戏库竖屏|GOG 为 bound 且有游戏|浏览并点击 GOG 游戏|账号区字段和列表结构符合 4.2；详情收到 `sourcePlatform=gog` 与正确游戏标识|P0|
|账号游戏库横屏|相同账号和游戏数据|从竖屏切为横屏|字段、列表数量和交互一致；保留滚动位置和账号状态|P0|
|账号游戏库空数据|接口成功返回 0 条|进入 GOG 账号游戏库|显示 empty 空态和刷新，不显示失败文案|P0|
|无缓存失败|模拟网络或接口失败|进入 GOG 账号游戏库|显示 error、重试和返回；重试成功后恢复内容|P0|
|有缓存失败|存在合法缓存且请求失败|进入 GOG 账号游戏库|显示 cached 内容、缓存时间和重试；不清空缓存|P0|
|授权失效|GOG token 已过期|进入账号卡或游戏库|显示 expired 和重新登录；成功后回原入口；其他平台不变|P0|
|GOG 来源详情|用户拥有 GOG 且账号可用|从 GOG 账号游戏库或搜索进入横竖详情|`sourcePlatform`、`selectedPlatform`、展示数据和启动按钮均为 GOG|P0|
|来源不可用|从 GOG 入口进入但 GOG 不可用|进入详情|保留 GOG 选择，提示绑定、重新登录或切换；不自动启动 Steam、EPIC|P0|
|无来源默认|入口未携带来源且三个平台均可用|进入详情|按 Steam > EPIC > GOG 选中 Steam|P0|
|详情平台与启动|用户拥有 Steam、EPIC、GOG 中至少一个平台版本|进入横竖详情并点击当前平台启动 icon|展示当前平台游戏时长、云存档和启动 icon；按当前平台参数启动；启动失败保留详情数据和平台选择|P0|
|切换启动平台|用户拥有多个平台|从 GOG 打开切换弹窗，选择 EPIC；再次打开后点击弹窗外部或取消|选择后平台标识、游戏时长、云存档和启动 icon 同步改为 EPIC；外部关闭或取消保持当前平台|P0|
|获得游戏标识|游戏存在 Steam、EPIC、GOG 获取渠道|查看横竖详情“获得游戏”区域并点击当前平台启动 icon|区域展示对应平台标识；标识只表示可获取渠道，不作为启动按钮；启动仍由详情卡片的启动 icon 承担|P0|
|游戏归类|存在中英文名称、别名、同名不同游戏或无法可靠归类的候选|执行映射|名称和别名只生成候选；仅确认的 `gameId` 与 `platformAppId` 关系合并；无法可靠归类或低置信度时进入不同详情|P0|
|竖屏搜索|搜索结果含 EPIC、GOG，且同一 PC 游戏有多个平台版本|查看并点击 GOG 结果|同一个 PC 游戏存在多个平台时，每个平台版本展示为独立结果；详情保留 GOG 来源|P0|
|横屏搜索|相同搜索数据|切换横屏并点击 GOG 结果|结果数量和来源参数与竖屏一致；布局无重叠和截断|P0|
|EPIC 评分换算|原始值分别为 0、4.4、5|展示搜索或详情评分|依次展示 0、8.8、10；结果限制在 0 至 10|P0|
|GOG 评分|GOG 没有可用评分|展示 GOG 搜索和详情|显示“暂无评分”，不伪造数值，不套用 EPIC 换算|P0|
|国内包|安装国内包|遍历 10 个页面视图|APP 名为盖世游戏；固定中文且不可切换；GOG 规则与海外包一致|P0|
|海外包|安装海外包并切换既有语言|遍历 10 个页面视图|APP 名为 GameHub；多语言可用；不出现 GaishiGame；GOG 规则与国内包一致|P0|
|安全存储|完成绑定、重启、退出|检查存储、日志、埋点和错误上报|无 GOG 邮箱、密码、官方页面输入内容和 token 原文；退出后 GOG token 清除|P0|
|域名允许名单|官方登录中跳转到非允许域名|继续授权|中止授权并提示；不加载未知页面|P0|
|恢复状态|分别模拟 loading、empty、error、expired、cancelled、cached|按页面提供的操作恢复|六类状态表现和恢复结果符合 4.2.12；回 normal 无需重启应用|P0|
|埋点|完成曝光、绑定、同步、切换和启动|检查事件与参数|事件不重不漏；失败类型可归因；不包含账号凭证和令牌|P0|
|灰度回退|GOG 授权或启动指标触发停放条件|关闭 GOG 功能|停止新入口和新绑定；Steam、EPIC 功能不受影响；已有 GOG 数据按确认后的回退策略处理|P0|

### 9.2 待确认项

#### 必须确认（阻塞开发）

|编号|状态|事项|默认建议|影响范围|正文引用|
|---|---|---|---|---|---|
|M1|[高风险假设]|GOG 官方是否提供可用于生产的授权、游戏库、令牌刷新、撤销和退出能力|开发前完成官方能力验证；任何能力不可用都不得以采集邮箱、密码替代|架构、开发、联调、安全、上线|2.1、4.2.2、4.2.14、5、7|
|M2|[待确认]|GOG 官方登录允许域名、回调域名和跳转边界|由服务端、客户端、安全共同维护最小允许名单，并覆盖地区域名和合法回调|客户端、安全、测试|4.2.2、4.2.14、5、9.1|
|M3|[高风险假设]|GOG 可返回的头像、用户名、GOG ID、游戏数、总时长、单游戏时长、云存档、游戏库字段及失败码范围|先取真实账号样本验证；未获官方支持的字段不得承诺；云存档缺失显示“未获取”，评分缺失显示“暂无评分”|服务端、客户端、测试、文案|4.2.1、4.2.5、4.2.6、4.2.14|
|M4|[待确认]|GOG 绑定与盖世游戏账号的关系，以及同一时间允许保留的 GOG 账号数|默认建议必须登录既有盖世游戏账号后绑定，单个盖世账号同一时间保留一个生效 GOG 账号|账号模型、迁移、测试|3.3、4.2.11、4.2.12|
|M5|[高风险假设]|游戏映射样本、人工确认责任和灰度准入准确率|默认建议高频游戏抽样准确率不低于 99%；低置信度不合并；产品确认异常映射|数据、搜索、详情、启动、上线|2.2、4.2.10、4.2.13、7|

#### 建议确认（影响完整度）

|编号|状态|事项|默认建议|影响范围|正文引用|
|---|---|---|---|---|---|
|S1|[高风险假设]|绑定成功率与首次同步成功率灰度阈值|默认分别采用 80%、90%，首轮灰度后按真实分布调整|灰度扩量、监控、复盘|2.2、3.4、6.3|
|S2|[待确认]|GOG 启动成功率比较基准和告警阈值|默认与同版本 EPIC、相同网络环境和设备范围对比，不直接使用无来源行业值|监控、灰度扩量|2.2、3.4、6.3|
|S3|[待确认]|游戏库排序、滚动加载、缓存时效和时间格式的既有准确口径|默认完全复用同版本 Steam、EPIC；开发在评审时引用现有实现与测试用例|客户端、测试|4.2.3、4.2.5、5|
|S4|[待确认]|海外包各语言的 GOG 业务文案和回退语言|默认由现有本地化流程翻译；缺少翻译时回退英语；品牌固定为 GameHub|本地化、测试、上线|2.3、4.2、5、7|
|S5|[待确认]|灰度比例、每阶段观察时间和停止条件|默认内部账号 → 小比例 → 分阶段扩量；每阶段至少覆盖完整 14 天观察窗口或足够样本|排期、运维、上线|3.5、6.3、7|

#### 可后续补充

|编号|状态|事项|默认建议|影响范围|正文引用|
|---|---|---|---|---|---|
|L1|[待确认]|GOG 启动增量的长期观察窗口|首版使用灰度前后各 14 天，数据稳定后再确定 30 天长期口径|产品复盘、后续版本判断|2.2、3.4、6.3|
|L2|[待确认]|继续增加平台后是否升级平台管理结构|本期保持方案 A；仅当新版游戏库既定入口空间不足或新增平台进入规划时重评|后续信息架构，不影响本期|3.2、4.1|

## 十、自检记录

### 10.1 模板与范围

- 已保留版本信息、背景与目标、故事介绍、概要设计、非功能需求、埋点需求、运营需求、上线后更新、验收与待确认项。
- 仅输出 4.2 详细设计（C端），未输出空的 B 端章节。
- 已覆盖国内包、海外包和 10 个 Demo 页面视图；横竖版分别还原并共用业务状态。
- 已增加复杂状态流转、数据字典、系统集成与降级说明。
- 本 PRD 无图示；交互与页面状态以同目录交付的单文件标注 Demo 为准。

### 10.2 前端展示易遗漏项

|清单项|检查结果|正文位置|
|---|---|---|
|列表排序、加载、空态、缓存、返回位置|GOG 账号游戏库复用 EPIC 规则，并区分 empty、error、cached|4.2.5、4.2.6、4.2.12|
|文字截断与多语言|游戏名沿用 EPIC 账号库截断；国内固定中文；海外支持既有多语言和 GameHub 品牌|4.2.5、4.2.6、5|
|输入与校验|客户端不提供 GOG 凭证输入；输入规则由 GOG 官方页负责；允许域名和回调需校验|4.2.2、5|
|图片与文件|本期无用户图片、文件上传，也不在 PRD 嵌入图片|4.2、10.1|
|状态与操作反馈|已列账号状态、六类模拟状态、合法流转、重复提交和危险操作确认|4.2.1、4.2.2、4.2.12|
|时间与数值|同步时间跟随设备时区；EPIC 评分换算覆盖 0、4.4、5；GOG 无可用评分时显示“暂无评分”|4.2.1、4.2.7、4.2.8、9.1|
|登录态与敏感数据|已覆盖未绑定、绑定、过期、取消、切换和退出；禁止保存、记录或上报凭证|4.2.1 至 4.2.6、5|
|弱网、并发、极端数据|已覆盖无缓存失败、有缓存失败、重复刷新、0 条游戏、低置信度映射|4.2.5、4.2.6、4.2.10、4.2.12、9.1|
|埋点与指标|已覆盖曝光、绑定、授权、同步、账号操作、游戏库、详情、搜索、启动和恢复|6.1 至 6.3|
|发布与降级|已定义技术验证、灰度、关闭开关和暂停扩量条件|3.5、7、9.1|

### 10.3 逻辑一致性

- 明确来源平台优先；来源不可用时保留来源并提示，未静默回退。
- 只有无来源时才按 Steam > EPIC > GOG 选择。
- 新版游戏库横竖入口顺序均为 EPIC → GOG → 导入游戏，未改变无关入口。
- GOG 账号游戏库的横竖结构参照现行 EPIC 页面；不渲染平台不具备的估值能力，也不保留对应空位。
- 已绑定账号的更新数据、切换账号和退出账号只收入右上角 `…` 菜单；仅 EPIC 在原操作区显示“喜加一”，GOG 不显示并自然收缩。
- 账号更新、切换、退出和过期只影响当前平台；菜单外部点击关闭、更新防重、切换失败保留旧账号、退出二次确认均已覆盖。
- 详情横竖版均覆盖游戏时长、云存档、平台切换弹窗、启动 icon 和启动失败恢复；“获得游戏”中的 Steam、EPIC、GOG 标识与启动入口语义分离。
- `gameId` 用于统一游戏，`platform + platformAppId` 用于平台版本；英文名称、中文名称和别名只作候选匹配，无法可靠归类或低置信度时进入不同详情。
- EPIC 评分按 5 分制转 10 分制；GOG 没有可用评分时显示“暂无评分”，不伪造数值。
- 所有无数据基线或外部能力结论均标注 [高风险假设] 或 [待确认]，并在 9.2 给出默认建议、影响范围和正文引用。

## 十一、模拟评审结果

|角色|结论|发现的问题|
|---|---|---|
|前端开发|✓ 通过|初审发现新版游戏库入口位置、横竖版独立还原、账号菜单收口、详情启动 icon 与“获得游戏”标识语义不够明确；已补入 4.2.1、4.2.3 至 4.2.10。|
|测试工程师|✓ 通过|初审发现 10 个页面视图、菜单外部关闭、更新防重、切换失败、退出二次确认、详情弹窗关闭、启动失败、缓存恢复和账号隔离缺少统一验收；已补入 4.2.12 与 9.1。|
|运营/业务方|✓ 通过|初审发现 GOG 官方能力、游戏映射抽样和灰度阈值缺少负责人及准入条件；已补入 7 和 9.2。|

**已自动补充的硬伤：** 新版游戏库入口顺序；10 个横竖页面视图；账号菜单与 EPIC“喜加一”位置；GOG 账号库移除账号价值、“喜加一”及其空位；搜索多平台独立结果；详情游戏时长、云存档、启动 icon、平台弹窗与“获得游戏”标识语义；来源不可用不静默回退；六类恢复状态；新账号同步完成后再替换；平台隔离验收；无法可靠归类时使用不同详情；GOG 官方能力验证；映射抽样和灰度准入。

**待用户确认的建议：** 绑定与同步阈值、GOG 启动对比基准、既有列表精确口径、海外多语言文案、灰度节奏。所有建议均已给出默认方案，不阻断 PRD 评审；其中 M1 至 M5 在开发或联调前必须关闭。
