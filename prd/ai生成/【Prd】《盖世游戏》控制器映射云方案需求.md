# 【Prd】《盖世游戏》控制器映射云方案需求

# 一、 版本信息

|**时间**|**版本**|**变更人**|**主要变更内容**|**备注**|
|---|---|---|---|---|
|2026\.6\.3|V1\.0|郑群超|创建文档||



# 二、 背景与目标

**背景：**
目前盖世游戏云游戏平台已支持虚拟按键布局编辑功能，用户可以自定义虚拟手柄按键的位置和大小。然而，部分PC游戏（如逃离塔科夫、CS2、Valorant等）原生不支持手柄操控，用户在云游戏环境中无法直接使用虚拟手柄进行游玩。

当前用户面临的核心痛点：不支持手柄的游戏只能通过触屏模拟鼠标键盘操作，体验极差，导致这类游戏在移动端的留存和时长远低于手柄适配游戏。

**目标：**
通过"控制器映射"功能，实现**虚拟手柄按键 → 键盘/鼠标操作**的映射绑定，让用户通过虚拟按键或物理手柄来操控原本不支持手柄的游戏。同时构建映射方案的**云分享生态**（推荐方案、排行榜、分享码），降低用户的配置门槛，提升整体云游戏体验。

**核心挑战：** 映射精准度（延迟<16ms）、方案数据同步一致性、物理手柄热插拔状态管理、分享码防滥用。

# 三、 故事介绍

## 3\.1 用户与运营场景

- **C端玩家（张三）：** 张三想在手机上玩《逃离塔科夫》，进入云游戏后看到右侧操作侧边栏的"控制器映射"功能已开启，点击"控制器映射设置"进入映射广场，在"推荐"Tab中找到"逃离塔科夫-标准"方案，点击查看详情后一键应用。虚拟按键立即按该方案的映射规则工作，A键映射鼠标左键开枪，摇杆映射WASD移动。

- **C端玩家（李四）：** 李四是FPS老手，觉得社区方案不够顺手，在"自定义"Tab中点击"创建"，选择"游戏专用"类型，命名为"塔科夫-速攻"，进入编辑器逐个配置按键映射关系，保存后通过"云分享"生成分享码`GS-A8K2M5`发给队友。

- **硬件场景（王五）：** 王五游戏中途插入Xbox手柄，系统自动切换为物理手柄输入（共享同一套映射方案），虚拟按键自动隐藏。拔出手柄后，虚拟按键立即恢复显示，无需手动操作。

## 3\.2 价值分析

- **扩大可玩游戏覆盖面：** 解锁平台上所有不支持手柄的PC游戏（FPS/RTS/MMO等），预计可新增30%+可玩游戏品类。

- **降低配置门槛：** 通过推荐方案和分享码机制，新手无需逐个配置按键映射，一键应用即可上手。

- **提升社区活跃：** 映射方案的分享与排行机制形成UGC内容生态，促进玩家间互动。

## 3\.3 核心体验路径

- **C端使用核心链路：**
侧边栏开启控制器映射 → 进入映射设置页 → 浏览推荐/排行/自定义方案 → 点击方案卡片查看详情 → 应用方案 → 虚拟按键按映射规则生效。

- **C端创建核心链路：**
自定义Tab → 点击"创建" → 输入方案名+选择类型 → 进入编辑器配置映射 → 保存 → 可选云分享生成分享码。

- **C端导入核心链路：**
自定义Tab → 点击"导入" → 输入分享码 → 方案自动添加到"我的方案" → 可直接应用。

## 3\.4 产品指标预测

1. **映射功能开启率：** 进入不支持手柄游戏的用户中，预计首月 `60%` 会开启控制器映射。

2. **方案应用率：** 进入映射设置页的用户中，预计 `75%` 会应用一个方案（推荐/排行/导入）。

3. **自定义创建率：** 预计 `15%` 用户会创建自定义方案。

4. **分享码使用率：** 预计 `8%` 用户会通过分享码导入方案。

## 3\.5 路径规划

- **当前版本（V1\.0）：** 实现控制器映射核心功能（启用/禁用、方案广场3Tab、方案详情与应用、创建与编辑、分享码导入导出、设备切换逻辑）。

- **未来可能升级（预告）：** 方案评分与评论系统、按游戏自动推荐映射方案、映射宏（组合键/连招）、方案版本历史。



# 四、概要设计

## 4\.1 模块设计

本次功能涉及的系统模块归纳如下：

- **【C端游戏侧边栏模块】**

    - **控制器映射开关：** 启用/禁用映射功能，联动虚拟按键显隐。

    - **设备列表：** 显示当前活跃输入设备（虚拟手柄/物理手柄），支持点击切换。

    - **引导说明：** "?"按钮弹窗解释功能用途。

- **【映射方案广场模块】**

    - **推荐Tab：** 适合当前游戏的方案 + 全部映射方案。

    - **排行Tab：** 按使用量排序的方案列表。

    - **自定义Tab：** 我的方案 + 我的云分享，含创建/导入操作。

- **【方案管理模块】**

    - **方案详情弹窗：** 查看方案信息与操作（应用/编辑/复制/删除/云分享）。

    - **方案创建：** 命名+类型选择（游戏专用/通用）→ 进入编辑器。

    - **方案编辑：** 复用按键布局编辑器，扩展映射绑定能力。

- **【云分享模块】**

    - **分享码生成：** 导出方案为唯一分享码。

    - **分享码导入：** 输入分享码获取方案到本地。

## 4\.2 详细设计

### 4\.2\.1 C端功能详细设计

|**模块**|**图示**|**展示与交互逻辑**|
|---|---|---|
|**游戏侧边栏**||- **控制器映射区标题行：**文案"控制器映射" + `?`引导按钮 + 启用/禁用开关<br>- 开关启用时：下方展开设备列表<br>- 开关禁用时：虚拟按键直接隐藏，设备区域折叠<br>- 开关Toast：启用→"控制器映射已启用"；禁用→"控制器映射已禁用"<br>- 开关状态持久化：全局粒度，跨游戏跨会话记录<br>- **设备列表（每个设备一组）：**<br>  - 标题行：设备名（"虚拟手柄"/"物理手柄"）+ 右侧">"箭头（点击进入该设备映射方案广场）<br>  - 方案名卡片：圆角边框卡片显示当前使用的方案名，右侧绿点表示该设备为当前激活态<br>  - 点击方案名卡片可切换激活设备（绿点跟随，虚拟按键显隐联动）<br>  - 切换Toast："已激活虚拟手柄"/"已激活物理手柄"<br>  - 每个设备独立一套映射方案，互不影响<br>  - 无手柄时仅显示"虚拟手柄"；手柄接入后增加"物理手柄"（不显示具体型号）<br>- **方案显隐规则：**设备类型目前只有一种"通用手柄"，虚拟手柄和物理手柄进入映射广场后展示同一套方案列表<br>- **其他侧边栏项：**摇杆灵敏度（滑块50%）、屏幕触摸方式（"直接触控"）、陀螺仪视角（"停用"）|
|**设备场景逻辑**||- 无手柄+映射开启：虚拟按键显示，设备列表仅"虚拟手柄"<br>- 无手柄+映射关闭：虚拟按键隐藏，设备区域折叠<br>- 手柄接入：自动激活物理手柄（绿点切到物理手柄），虚拟按键隐藏，设备列表增加"物理手柄"；Toast"手柄已连接，已切换到物理手柄"<br>- 手柄拔出：**始终恢复虚拟按键显示**（硬件可用性>用户意图）；设备列表回到仅虚拟手柄；Toast"手柄已断开，虚拟按键已恢复显示"<br>- 手动切换到虚拟手柄（有手柄时）：虚拟按键显示，物理手柄非活跃<br>- 手动切换到物理手柄：虚拟按键隐藏|
|**?引导弹窗**||- 触发：点击侧边栏`?`按钮<br>- 样式：居中弹窗，右上角×关闭，无底部按钮<br>- 标题文案："控制器映射"<br>- 正文文案："部分游戏（如逃离塔科夫、CS2等）不支持手柄操控。开启此功能后，可将虚拟手柄的每个按键映射到键盘/鼠标操作，从而通过虚拟按键或物理手柄来游玩这些游戏。\n\n每个手柄（虚拟手柄、物理手柄）可独立设置各自的映射方案，互不影响。点击设备右侧的'>'即可进入对应手柄的映射设置。\n\n你可以选择社区推荐方案，也可以自定义映射配置，并通过分享码与好友共享。"<br>- 关闭：点击×或遮罩|
|**映射方案广场页**||- **页面头部：**返回按钮 + 标题"控制器映射" + 右上角搜索icon<br>- 搜索展开：placeholder"搜索映射方案"，右侧"取消"；支持方案名/作者模糊匹配；结果文案"搜索结果：X 个方案"；无结果"没有找到相关方案"<br>- **Tab栏：**推荐 / 排行 / 自定义<br>- **推荐Tab：**区域一标题"适合「当前游戏」的映射方案"（动态读取当前游戏名）+ 右对齐"查看全部 ›"（跳游戏专用方案子页）；若无匹配方案则该区域整体不显示；区域二标题"全部映射方案"<br>- **排行Tab：**所有方案按使用量降序；支持搜索<br>- **自定义Tab：**子Tab"我的方案"/"我的云分享" + 右对齐"+ 创建""导入"按钮；空态"暂无自定义方案"/"暂无云分享"；自定义Tab不显示搜索icon<br>- **方案卡片（列表样式）：**手柄icon + 方案名 + 作者·使用次数（"1.8万次使用"）+ 右侧箭头；正在使用的方案右上角显示"应用中"标签；点击进入方案详情弹窗|
|**游戏专用方案子页**||- 从推荐页"查看全部"进入<br>- 头部：返回按钮 + 标题"游戏专用方案"<br>- 搜索框：placeholder"搜索游戏专用方案"，实时过滤<br>- 展示所有游戏专用方案；无结果"没有找到相关方案"|
|**方案详情弹窗**||- 样式：居中弹窗，点击遮罩或×关闭<br>- 内容：缩略图（手柄布局占位图）+ 方案名 + "作者：XXX · X万次使用"<br>- **我的方案操作（一排icon）：**应用→Toast"方案已应用"，关闭弹窗+广场页，侧边栏方案名更新；编辑→跳转编辑页；复制→Toast"已复制方案"；删除→Toast"已删除"并关闭弹窗；云分享→拉起导出分享码弹窗<br>- **他人方案操作（一排icon）：**应用 / 编辑（=复制到我的方案+进编辑器，Toast"已复制到我的方案并进入编辑"）/ 复制|
|**创建/保存方案弹窗**||- **触发：**自定义Tab "+ 创建" → 直接进入编辑器 → 编辑器点"保存"时弹出此弹窗<br>- 样式：居中弹窗，右上角×关闭<br>- 标题文案："保存映射方案"<br>- 方案名称：标签"方案名称"，输入框placeholder"输入方案名称"，限10字，右侧实时显示"x/10"字数统计<br>- 方案类型：标签"方案类型"，平铺"游戏专用"/"通用方案"，默认选中"游戏专用"（绿色边框）<br>- 按钮文案："确认保存"<br>- 校验Toast：空→"请输入方案名称"；超10字→"方案名称不超过10个字"<br>- 保存成功→关闭弹窗+编辑页，刷新方案列表，Toast"方案已保存"|
|**导出分享码弹窗**||- 触发：我的方案详情点击"云分享"<br>- 样式：居中弹窗，右上角×关闭<br>- 标题文案："发布到云端"<br>- 方案名称：预填当前方案名，可修改，限10字，右侧"x/10"字数统计<br>- 按钮文案："确认发布"<br>- **敏感词检测：**提交时进行敏感词过滤，不通过→Toast"内容包含违规词汇，请修改后重新提交"<br>- 发布成功Toast："发布成功，方案已上传到云端"<br>- 发布后方案出现在"我的云分享"列表中，同时可被其他用户在推荐/排行中看到|
|**导入分享码弹窗**||- 触发：自定义Tab "导入"按钮<br>- 标题文案："导入分享码"<br>- 输入框placeholder："输入分享码，如 GS-X7K9M2"<br>- 按钮1："确认导入"（绿色）<br>- 按钮2："取消"（文字按钮）<br>- 校验Toast：空→"请输入分享码"<br>- 成功Toast："导入成功！方案已添加到"我的方案""<br>- 失败Toast："分享码无效，请检查后重试"<br>- 说明：分享码由发布方案时系统自动生成，用户通过社交渠道获取后在此处输入导入|
|**输入映射编辑页**||- 触发：创建时直接进入 / 详情点击"编辑"<br>- 头部：返回按钮 + 标题"方案名 - 输入映射"（新建时显示"新建方案 - 输入映射"）+ 右侧"保存"（绿色）<br>- 编辑区域：复用按键布局编辑器（拖拽、添加按键、撤销/重做）<br>- 新建方案保存→弹出保存弹窗（填名称+类型）<br>- 已有方案保存→Toast"已保存"并关闭编辑页<br>- 返回逻辑：有未保存修改时提示"放弃编辑？"（确认/取消）|

## 4\.4 非功能需求

|**需求类型**|**详细要求**|
|---|---|
|**性能**|1\. 映射响应延迟 < 16ms（1帧），确保操作手感。<br>2\. 方案列表首屏加载 < 1s。<br>3\. 设备切换（手柄接入/拔出）响应时间 < 200ms。|
|**兼容性**|1\. 支持Android 9\.0+、iOS 13\.0+。<br>2\. 支持主流物理手柄：Xbox系列、PS系列、Switch Pro、北通等。<br>3\. 横竖屏自适应：横屏一排4卡片，竖屏一排2卡片。|
|**用户体验**|1\. 手柄热插拔全自动处理，无需用户手动操作。<br>2\. 方案应用后即时生效，无需重启游戏。<br>3\. 所有操作均有Toast反馈（成功/失败）。<br>4\. 危险操作（删除方案）需二次确认。|
|**数据安全**|1\. 分享码不可逆向推导方案内容。<br>2\. 方案数据云端同步，设备丢失后可恢复。<br>3\. 分享码生成频率限制：单用户每日最多生成50个。|

---

# 五、交付设计（数据分析与埋点设计）

## 5\.1 核心业务指标

- **功能漏斗：** 进入游戏（UV） → 开启控制器映射（UV） → 进入映射设置页（UV） → 应用方案（UV） → 完成一局游戏（UV）。

## 5\.2 埋点事件表

|**事件ID**|**事件名称**|**触发时机**|**关键参数**|
|---|---|---|---|
|`mapping_toggle`|控制器映射开关|用户切换启用/禁用开关时|`uid`, `action`(enable/disable), `game_id`|
|`mapping_page_view`|映射设置页曝光|进入映射方案广场时|`uid`, `game_id`, `source`(sidebar)|
|`mapping_tab_switch`|Tab切换|切换推荐/排行/自定义Tab时|`uid`, `tab_name`(recommend/rank/custom)|
|`scheme_card_click`|方案卡片点击|点击方案卡片拉起详情弹窗时|`uid`, `scheme_id`, `scheme_type`, `tab_source`|
|`scheme_apply`|应用方案|点击详情弹窗"应用"按钮时|`uid`, `scheme_id`, `scheme_name`, `game_id`|
|`scheme_create`|创建方案|确认创建方案时|`uid`, `scheme_name`, `scope`(game/general)|
|`scheme_share_export`|导出分享码|生成分享码时|`uid`, `scheme_id`, `share_code`|
|`scheme_share_import`|导入分享码|确认导入分享码时|`uid`, `share_code`, `result`(success/fail)|
|`device_switch`|设备切换|手柄接入/拔出/手动切换时|`uid`, `device_type`(virtual/physical), `trigger`(auto/manual)|
|`scheme_search`|方案搜索|执行搜索动作时|`uid`, `keyword`, `result_count`|
|`guide_view`|引导弹窗曝光|点击?按钮展示引导弹窗时|`uid`, `game_id`|

## 5\.3 埋点参数表

|**参数名**|**类型**|**是否必填**|**参数说明**|**枚举值 / 示例**|
|---|---|---|---|---|
|**uid**|string|是|当前登录用户唯一标识|`u882910`|
|**action**|string|是|开关操作动作|`enable`（启用）, `disable`（禁用）|
|**game_id**|string|是|当前云游戏的唯一标识ID|`tarkov_001`, `cs2_002`|
|**source**|string|否|触发来源页面|`sidebar`（侧边栏）|
|**tab_name**|string|否|当前所在Tab名称|`recommend`（推荐）, `rank`（排行）, `custom`（自定义）|
|**scheme_id**|string|否|方案唯一标识ID|`sch_20260603_001`|
|**scheme_name**|string|否|方案名称|`逃离塔科夫-标准`|
|**scheme_type**|string|否|方案归属类型|`recommend`（推荐）, `rank`（排行）, `mine`（我的）, `cloud`（云分享）|
|**scope**|string|否|方案适用范围|`game`（游戏专用）, `general`（通用）|
|**tab_source**|string|否|点击卡片时所在的Tab来源|`recommend`, `rank`, `custom_mine`, `custom_cloud`|
|**share_code**|string|否|分享码值|`GS-X7K9M2`|
|**result**|string|否|操作结果|`success`（成功）, `fail`（失败）|
|**device_type**|string|否|设备类型|`virtual`（虚拟手柄）, `physical`（物理手柄）|
|**trigger**|string|否|设备切换触发方式|`auto`（自动，手柄插拔）, `manual`（手动点击）|
|**keyword**|string|否|搜索关键词|`塔科夫`, `CS2`|
|**result_count**|int|否|搜索结果数量|`3`, `0`|
|**layout_type**|string|否|手柄布局类型|`switch`, `xbox`|

# 六、来自功能上线后的更新

**上线后记录。**