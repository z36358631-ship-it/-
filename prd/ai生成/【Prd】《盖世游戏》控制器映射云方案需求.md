# 【Prd】《盖世游戏》控制器映射云方案需求

# 一、 版本信息

|**时间**|**版本**|**变更人**|**主要变更内容**|**备注**|
|---|---|---|---|---|
|2026\.6\.3|V1\.0|郑群超|创建文档||



# 二、 背景与目标

**背景：**
目前盖世游戏云游戏平台已支持虚拟按键布局编辑功能，用户可以自定义虚拟手柄按键的位置和大小。然而，部分PC游戏（如逃离塔科夫、CS2、Valorant等）原生不支持手柄操控，用户在云游戏环境中无法直接使用虚拟手柄进行游玩。

当前用户面临的核心痛点：不支持手柄的游戏只能通过触屏模拟鼠标键盘操作，体验极差，导致这类游戏在移动端的留存和时长远低于手柄适配游戏。

**目标：**
通过"控制器映射"功能，实现**虚拟手柄按键 → 键盘/鼠标操作**的映射绑定，让用户通过虚拟按键或物理手柄来操控原本不支持手柄的游戏。同时构建映射方案的**云分享生态**（推荐方案、排行榜、分享码），降低用户的配置门槛，提升整体云游戏体验。

**核心挑战：** 映射精准度（延迟<16ms）、方案数据同步一致性、物理手柄热插拔状态管理、分享码防滥用。

# 三、 故事介绍

## 3\.1 用户与运营场景

- **C端玩家（张三）：** 张三想在手机上玩《逃离塔科夫》，进入云游戏后看到右侧操作侧边栏的"控制器映射"功能已开启，点击"控制器映射设置"进入映射广场，在"推荐"Tab中找到"逃离塔科夫-标准"方案，点击查看详情后一键应用。虚拟按键立即按该方案的映射规则工作，A键映射鼠标左键开枪，摇杆映射WASD移动。

- **C端玩家（李四）：** 李四是FPS老手，觉得社区方案不够顺手，在"自定义"Tab中点击"创建"，选择"游戏专用"类型，命名为"塔科夫-速攻"，进入编辑器逐个配置按键映射关系，保存后通过"云分享"生成分享码`GS-A8K2M5`发给队友。

- **硬件场景（王五）：** 王五游戏中途插入Xbox手柄，系统自动切换为物理手柄输入（共享同一套映射方案），虚拟按键自动隐藏。拔出手柄后，虚拟按键立即恢复显示，无需手动操作。

## 3\.2 价值分析

- **扩大可玩游戏覆盖面：** 解锁平台上所有不支持手柄的PC游戏（FPS/RTS/MMO等），预计可新增30%+可玩游戏品类。

- **降低配置门槛：** 通过推荐方案和分享码机制，新手无需逐个配置按键映射，一键应用即可上手。

- **提升社区活跃：** 映射方案的分享与排行机制形成UGC内容生态，促进玩家间互动。

## 3\.3 核心体验路径

- **C端使用核心链路：**
侧边栏开启控制器映射 → 进入映射设置页 → 浏览推荐/排行/自定义方案 → 点击方案卡片查看详情 → 应用方案 → 虚拟按键按映射规则生效。

- **C端创建核心链路：**
自定义Tab → 点击"创建" → 输入方案名+选择类型 → 进入编辑器配置映射 → 保存 → 可选云分享生成分享码。

- **C端导入核心链路：**
自定义Tab → 点击"导入" → 输入分享码 → 方案自动添加到"我的方案" → 可直接应用。

## 3\.4 产品指标预测

1. **映射功能开启率：** 进入不支持手柄游戏的用户中，预计首月 `60%` 会开启控制器映射。

2. **方案应用率：** 进入映射设置页的用户中，预计 `75%` 会应用一个方案（推荐/排行/导入）。

3. **自定义创建率：** 预计 `15%` 用户会创建自定义方案。

4. **分享码使用率：** 预计 `8%` 用户会通过分享码导入方案。

## 3\.5 路径规划

- **当前版本（V1\.0）：** 实现控制器映射核心功能（启用/禁用、方案广场3Tab、方案详情与应用、创建与编辑、分享码导入导出、设备切换逻辑）。

- **未来可能升级（预告）：** 方案评分与评论系统、按游戏自动推荐映射方案、映射宏（组合键/连招）、方案版本历史。



# 四、概要设计

## 4\.1 模块设计

本次功能涉及的系统模块归纳如下：

- **【C端游戏侧边栏模块】**

    - **控制器映射开关：** 启用/禁用映射功能，联动虚拟按键显隐。

    - **设备列表：** 显示当前活跃输入设备（虚拟手柄/物理手柄），支持点击切换。

    - **引导说明：** "?"按钮弹窗解释功能用途。

- **【映射方案广场模块】**

    - **推荐Tab：** 适合当前游戏的方案 + 全部映射方案。

    - **排行Tab：** 按使用量排序的方案列表。

    - **自定义Tab：** 我的方案 + 我的云分享，含创建/导入操作。

- **【方案管理模块】**

    - **方案详情弹窗：** 查看方案信息与操作（应用/编辑/复制/删除/云分享）。

    - **方案创建：** 命名+类型选择（游戏专用/通用）→ 进入编辑器。

    - **方案编辑：** 复用按键布局编辑器，扩展映射绑定能力。

- **【云分享模块】**

    - **分享码生成：** 导出方案为唯一分享码。

    - **分享码导入：** 输入分享码获取方案到本地。

## 4\.2 功能清单结构

|**一级模块**|**二级功能**|**优先级**|
|---|---|---|
|游戏侧边栏|控制器映射开关|P0|
|游戏侧边栏|设备列表与切换|P0|
|游戏侧边栏|引导说明弹窗|P2|
|映射方案广场|推荐Tab（当前游戏+全部）|P0|
|映射方案广场|排行Tab|P1|
|映射方案广场|自定义Tab（我的+云分享）|P0|
|映射方案广场|搜索功能|P1|
|映射方案广场|游戏专用方案子页|P1|
|方案管理|方案详情弹窗|P0|
|方案管理|应用方案|P0|
|方案管理|创建方案（游戏专用/通用）|P0|
|方案管理|编辑方案（复用布局编辑器）|P0|
|方案管理|复制方案|P1|
|方案管理|删除方案|P1|
|云分享|导出分享码|P0|
|云分享|导入分享码|P0|
|设备管理|手柄接入自动切换|P0|
|设备管理|手柄拔出恢复虚拟按键|P0|
|设备管理|按键自定义布局（Switch/Xbox）|P1|

## 4\.3 详细设计

### 4\.3\.1 游戏侧边栏（C端）

|**模块名称**|**展示&交互说明**|
|---|---|
|**控制器映射区**|- **标题行：**"控制器映射" + `?`引导按钮 + 启用/禁用开关<br>- **开关启用时：**下方展开设备列表 + "控制器映射设置"入口行<br>- **开关禁用时：**虚拟按键直接隐藏，设备区域折叠不显示<br>- **开关状态持久化：**全局粒度，跨游戏跨会话记录|
|**设备列表**|- **无手柄时：**仅显示"虚拟手柄"，状态"使用中"，绿色高亮边框<br>- **有手柄时：**显示"虚拟手柄" + "物理手柄"（不显示具体型号名称）<br>- **切换交互：**点击设备卡片切换当前启用设备，选中态带高亮边框和绿色圆点<br>- **共享映射：**虚拟手柄和物理手柄共用同一套映射方案|
|**按键自定义布局（手柄连接后显示）**|- 仅在物理手柄连接后显示<br>- 标题"按键自定义布局"<br>- 平铺两个选项：Switch布局 / Xbox布局，默认选中Switch<br>- 选中态：绿色边框+背景高亮<br>- 手柄拔出后该区域自动隐藏|
|**控制器映射设置行**|- 右侧箭头左边显示当前引用的方案名（如"逃离塔科夫-标准"）<br>- 点击箭头进入映射方案广场页|
|**?引导弹窗**|- 居中弹窗 + 右上角×关闭<br>- 内容：说明功能用途（不支持手柄的游戏可以设置映射来玩）<br>- 无"知道了"按钮，仅×关闭|

### 4\.3\.2 设备场景逻辑

|**场景**|**系统行为**|
|---|---|
|**无手柄 + 映射开启**|虚拟按键显示，设备列表仅"虚拟手柄（使用中）"|
|**无手柄 + 映射关闭**|虚拟按键隐藏，设备区域折叠|
|**手柄接入**|自动切换为物理手柄输入，虚拟按键隐藏，设备列表显示两个设备（物理手柄高亮），显示"按键自定义布局"选项（Switch/Xbox）|
|**手柄拔出**|无论用户是否手动关过映射，**始终恢复虚拟按键显示**（硬件可用性 > 用户意图）；设备列表回到仅虚拟手柄；"按键自定义布局"选项隐藏|
|**手动切换到虚拟手柄（有手柄时）**|虚拟按键显示，物理手柄仍连接但为非活跃态|
|**手动切换到物理手柄**|虚拟按键隐藏，物理手柄为活跃态|

### 4\.3\.3 映射方案广场（C端）

|**模块名称**|**展示&交互说明**|
|---|---|
|**页面头部**|- 标题"控制器映射" + 返回按钮<br>- 右上角搜索icon，点击展开搜索栏，支持按方案名/作者模糊搜索|
|**推荐Tab**|- **适合当前游戏区域：**标题"适合「XX游戏」的映射方案" + 右对齐"查看全部 ›"<br>- 点击"查看全部"跳转游戏专用方案子页<br>- **全部映射方案区域：**展示所有推荐方案<br>- 横屏一排4个方案卡片，竖屏一排2个|
|**排行Tab**|- 按使用量降序排列所有方案<br>- 卡片样式与推荐页一致|
|**自定义Tab**|- 子Tab切换：我的方案 / 我的云分享<br>- 子Tab右对齐"+ 创建"按钮和"导入"按钮<br>- 空状态：居中显示"暂无自定义方案"/"暂无云分享"|
|**方案卡片**|- 展示：缩略图（布局预览） + 方案名（最多1行截断） + 作者 · 使用次数<br>- 点击：拉起居中弹窗式方案详情|
|**游戏专用方案子页**|- 从推荐页"查看全部"进入<br>- 带搜索框，展示所有scope=game的方案<br>- 返回按钮回到映射广场|

### 4\.3\.4 方案详情弹窗（C端）

|**模块名称**|**展示&交互说明**|
|---|---|
|**弹窗样式**|- 居中弹窗（非底部拉起），点击遮罩或×关闭<br>- 展示：缩略图 + 方案名 + 作者·使用量·关联游戏|
|**分享码展示**|- 若方案有分享码，单独行展示分享码值|
|**操作按钮（我的方案）**|- 一排圆形icon按钮：应用 / 编辑 / 复制 / 删除 / 云分享<br>- 删除：Toast提示"已删除"并关闭弹窗<br>- 云分享：生成分享码弹窗|
|**操作按钮（他人方案）**|- 一排圆形icon按钮：应用 / 复制 / 分享码|
|**应用方案**|- 点击后：侧边栏方案名更新 + Toast"方案已应用" + 自动关闭弹窗和广场页返回游戏|

### 4\.3\.5 方案创建（C端）

|**模块名称**|**展示&交互说明**|
|---|---|
|**创建弹窗**|- 居中弹窗 + 右上角×关闭<br>- **方案名称：**输入框，**限制10个字**，placeholder"输入方案名称"<br>- **方案类型：**一行两个选项"游戏专用" / "通用方案"，默认选中"游戏专用"，选中态绿色边框<br>- **确认创建按钮**|
|**创建后跳转**|- 创建成功后自动跳转到编辑页（复用按键布局编辑器）<br>- 编辑页标题显示"方案名 - 输入映射"|
|**校验规则**|- 方案名为空：Toast"请输入方案名称"<br>- 方案名超过10字：Toast"方案名称不超过10个字"|

### 4\.3\.6 云分享（C端）

|**模块名称**|**展示&交互说明**|
|---|---|
|**导出分享码**|- 居中弹窗展示生成的分享码（格式：`GS-XXXXXX`，6位大写字母数字）<br>- 说明文案："将此分享码发送给好友，对方在'导入分享码'中输入即可使用"<br>- "复制分享码"按钮 + "关闭"按钮|
|**导入分享码**|- 居中弹窗 + 输入框（placeholder"输入分享码，如 GS-X7K9M2"）<br>- "确认导入"按钮<br>- 导入成功：Toast"导入成功！方案已添加到'我的方案'"<br>- 输入为空：Toast"请输入分享码"|
|**分享码规则**|- 唯一性：每个方案生成唯一分享码<br>- 格式：`GS-` + 6位随机大写字母数字<br>- 有效期：永久有效|

## 4\.4 非功能需求

|**需求类型**|**详细要求**|
|---|---|
|**性能**|1\. 映射响应延迟 < 16ms（1帧），确保操作手感。<br>2\. 方案列表首屏加载 < 1s。<br>3\. 设备切换（手柄接入/拔出）响应时间 < 200ms。|
|**兼容性**|1\. 支持Android 9\.0+、iOS 13\.0+。<br>2\. 支持主流物理手柄：Xbox系列、PS系列、Switch Pro、北通等。<br>3\. 横竖屏自适应：横屏一排4卡片，竖屏一排2卡片。|
|**用户体验**|1\. 手柄热插拔全自动处理，无需用户手动操作。<br>2\. 方案应用后即时生效，无需重启游戏。<br>3\. 所有操作均有Toast反馈（成功/失败）。<br>4\. 危险操作（删除方案）需二次确认。|
|**数据安全**|1\. 分享码不可逆向推导方案内容。<br>2\. 方案数据云端同步，设备丢失后可恢复。<br>3\. 分享码生成频率限制：单用户每日最多生成50个。|

---

# 五、交付设计（数据分析与埋点设计）

## 5\.1 核心业务指标

- **功能漏斗：** 进入游戏（UV） → 开启控制器映射（UV） → 进入映射设置页（UV） → 应用方案（UV） → 完成一局游戏（UV）。

## 5\.2 埋点事件表

|**事件ID**|**事件名称**|**触发时机**|**关键参数**|
|---|---|---|---|
|`mapping_toggle`|控制器映射开关|用户切换启用/禁用开关时|`uid`, `action`(enable/disable), `game_id`|
|`mapping_page_view`|映射设置页曝光|进入映射方案广场时|`uid`, `game_id`, `source`(sidebar)|
|`mapping_tab_switch`|Tab切换|切换推荐/排行/自定义Tab时|`uid`, `tab_name`(recommend/rank/custom)|
|`scheme_card_click`|方案卡片点击|点击方案卡片拉起详情弹窗时|`uid`, `scheme_id`, `scheme_type`, `tab_source`|
|`scheme_apply`|应用方案|点击详情弹窗"应用"按钮时|`uid`, `scheme_id`, `scheme_name`, `game_id`|
|`scheme_create`|创建方案|确认创建方案时|`uid`, `scheme_name`, `scope`(game/general)|
|`scheme_share_export`|导出分享码|生成分享码时|`uid`, `scheme_id`, `share_code`|
|`scheme_share_import`|导入分享码|确认导入分享码时|`uid`, `share_code`, `result`(success/fail)|
|`device_switch`|设备切换|手柄接入/拔出/手动切换时|`uid`, `device_type`(virtual/physical), `trigger`(auto/manual)|
|`scheme_search`|方案搜索|执行搜索动作时|`uid`, `keyword`, `result_count`|
|`guide_view`|引导弹窗曝光|点击?按钮展示引导弹窗时|`uid`, `game_id`|

# 六、来自功能上线后的更新

**上线后记录。**