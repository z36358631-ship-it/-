# 运营数据看板 — 埋点事件需求表

> 生成日期：2026-07-04
> 更新日期：2026-07-07
> 说明：对照「运营数据看板需求.md」7大模块所需指标，逐项标注数据来源事件的覆盖状态。
> - ✅ 已有：bi-events.md 或 6.0.9 链路埋点已上报
> - ⚠️ 需改造：事件已有但缺少必要参数或需扩展
> - 🆕 需新增：当前无对应事件，需要补充埋点
>
> **原则：PRD 中已定义的事件（社区首页/我的页/游戏详情页/新手引导/登录优化）视为已排期，不重复提需求，仅在备注中标注来源。**

---

## 一、工作台（大盘数据总览）

| 看板指标 | 所需事件 | 状态 | 现有位置/说明 |
|---------|---------|------|-------------|
| 活跃用户（所选时间范围去重device_id，7天/30天为去重总数非日均） | `app_launch` | ✅已有 | bi-events「App/服务端事件」；6.0.9已追加公参(`register_date`, `app_version`, `screen_orientation`) |
| 当日新增用户 | `app_launch` + `is_first_launch` | ✅已有 | 6.0.9链路埋点阶段一#1，通过 `is_first_launch=true` 筛选 |
| 昨日留存率 | `app_launch` 按 device_id 次日回访计算 | ✅已有 | 后端离线计算，无需额外埋点 |
| 人均互动时长（每位活跃用户平均session时长，单位分钟） | session 时长（`app_launch` 冷启动到最后事件时间差） | ⚠️需改造 | 当前无独立 `session_end` 事件；**建议：后端根据最后一条事件时间戳倒推 session 时长，无需新增客户端埋点** |
| 流失用户数 | `app_launch` 反向计算（15天无上报） | ✅已有 | 后端离线任务，按 device_id 最后 app_launch 时间判定 |
| 活跃游戏数 | `game_play_success` | ✅已有 | 6.0.9链路埋点阶段四#20，去重 game_id 即可 |
| 游戏用户数 | `game_play_success` | ✅已有 | 去重 user_id |
| 游戏总时长 | `game_session` / `game_usage` | ✅已有 | bi-events「PC/Wine游戏内与会话」 |
| 游戏启动成功率 | `game_start_click` + `game_play_success` + `game_play_fail` | ✅已有 | 6.0.9链路埋点#19/#20/#21 |
| 充值总额 | `pay_result` (is_success=true, amount) | ✅已有 | bi-events「充值/支付」 |
| 付费用户数 | `pay_result` (is_success=true) 去重 user_id | ✅已有 | 同上 |
| 人均云游时长 | `game_session` (game_type=cloud) 或 `cloud_game_enter` + session | ✅已有 | bi-events + 6.0.9#12 `cloud_game_enter` |
| 社区活跃用户数 | `community_page_view` | ✅已有 | 社区首页PRD已定义，去重 uid |
| 新增内容数 | `post_publish_submit` | ✅已有 | 社区首页PRD已定义 |
| 渠道/版本/语言/地区分布 | 公参 `xj_channel` + `app_version` + 设备信息 | ✅已有 | 通用公参链路已有；语言/地区需设备系统字段 |
| 启动类型分布（PC启动/云游启动/社区活跃，去掉"其他"） | `game_start_click` + `cloud_game_enter` + `community_page_view` | ✅已有 | 各事件按 game_type 分组 |
| 功能渗透率（含"竖屏功能"） | 各功能对应事件 / 活跃用户数 | ✅已有 | 计算指标 |
| 核心日报（底部加"日均"行+加"同比"列） | 综合计算 | ✅已有 | 后端聚合 |

### 大盘模块缺口（需新增）

| 指标 | 缺失说明 | 建议新增事件/方案 |
|------|---------|----------------|
| 硬件类型数量 | 需统计活跃的芯片/机型种类数 | 🆕 建议在公参中追加 `chip_model`(规范化后) 和 `device_model`(规范化后) 字段，后端做字典映射清洗后统计 |
| 系统语言分布 | 当前公参无 `system_language` | 🆕 建议公参追加 `system_language` (如 zh-CN, en-US) |
| 国家/地区分布 | 当前公参无 `country_code` | 🆕 建议公参追加 `country_code` (通过 IP 或系统 locale 获取) |

---

## 二、用户生命周期

| 看板指标 | 所需事件 | 状态 | 现有位置/说明 |
|---------|---------|------|-------------|
| 活跃用户数/账号数 | `app_launch` 去重 device_id / user_id | ✅已有 | — |
| 新增用户数/账号数 | `app_launch` + `is_first_launch` | ✅已有 | 6.0.9#1 |
| 游客用户数 | `login_result` (method=guest) 或 `guest_mode_click` | ✅已有 | 6.0.9#9 login_result 可过滤 method；登录优化PRD 有 `guest_mode_click` |
| 匿名设备数 | `app_launch` 中 login_status=false 的 device_id | ✅已有 | 公参 `login_status` 已存在 |
| 整体活跃留存矩阵 | `app_launch` 按日期回访 | ✅已有 | 后端离线任务 |
| 新/老用户维度留存 | `app_launch` + `register_date` 区分新老 | ✅已有 | 6.0.9已追加 `register_date` 公参 |
| 流失用户数趋势 | `app_launch` 反向（15天无上报） | ✅已有 | 后端离线任务 |
| 流失用户最后事件分布 | 全量事件中按 device_id 取最后 N 条 | ✅已有 | 后端离线查询，无需额外埋点 |

### 用户模块缺口

| 指标 | 缺失说明 | 建议方案 |
|------|---------|---------|
| 游客用户 session 标记 | 需区分"游客态 session"和"未登录 session" | ⚠️ 建议在 `app_launch` 公参中追加 `account_type` 枚举：`registered` / `guest` / `anonymous`，替代单纯的 boolean `login_status` |

---

## 三、游戏生态数据

| 看板指标 | 所需事件 | 状态 | 现有位置/说明 |
|---------|---------|------|-------------|
| PC游戏累计总量 | 服务端游戏库数据 | ✅已有 | 非埋点，后端数据库直接查询 |
| 新增游戏数 | 服务端游戏入库记录 | ✅已有 | 同上 |
| 活跃游戏数 | `game_play_success` 去重 game_id | ✅已有 | 6.0.9#20 |
| 人均游戏时长 | `game_session` 总时长 / 活跃用户数 | ✅已有 | 计算指标，基于已有 game_session 事件 |
| 下载游戏数 | `game_download_complete` (is_success=true) 去重 game_id | ✅已有 | 6.0.9#16 |
| 导入游戏数 | `game_import_result` (is_success=true) 去重 game_id | ✅已有 | 6.0.9#18 |
| 成功运行游戏数(≥5min) | `game_play_success` (launch_duration + session > 5min) | ✅已有 | 6.0.9#20 `game_play_success` 定义为"画面渲染成功且持续≥5秒"，但5分钟判定需结合 `game_session` duration≥300s |
| 游戏排行榜-总时长 | `game_session` + `game_usage` 按 game_id 聚合 | ✅已有 | bi-events |
| 游戏排行榜-用户数 | `game_play_success` 按 game_id 去重 user_id | ✅已有 | — |
| 游戏排行榜-启动成功设备数 | `game_play_success` 按 game_id 去重 device_id | ✅已有 | — |
| 游戏排行榜-启动成功率 | `game_play_success` / (`game_play_success` + `game_play_fail`) | ✅已有 | 6.0.9#20/#21 |
| 云游活跃/新增用户 | `cloud_game_enter` 去重 user_id；首次=新增 | ✅已有 | 6.0.9#12 |
| 充值订单/用户/金额 | `pay_result` | ✅已有 | bi-events「充值/支付」 |

### 游戏模块缺口

无新增埋点需求。所有指标均可通过现有事件 + 后端计算覆盖。

---

## 四、社区平台数据

| 看板指标 | 所需事件 | 状态 | 现有位置/说明 |
|---------|---------|------|-------------|
| 社区活跃用户数 | `community_page_view` 去重 uid | ✅已有 | 社区首页PRD已定义 |
| 新增帖子数 | `post_publish_submit` 计数 | ✅已有 | 社区首页PRD已定义 |
| 新增话题数 | 服务端话题创建记录 | ✅已有 | 后端数据库直接查询 |
| 帖子访问总量 | `post_click` 计数 | ✅已有 | 社区首页PRD已定义 |
| 人均帖子阅读数 | `post_click` / 活跃用户数 | ✅已有 | — |
| 人均停留时长 | 需社区页停留时长 | 🆕需新增 | 见下方 |
| 人均互动事件 | `post_interaction` 计数 / 活跃用户数 | ✅已有 | 社区首页PRD已定义 |
| 互动总量/分类型曲线 | `post_interaction` 按 action_type 分组 | ✅已有 | 社区首页PRD: action_type = like/fav/comment/share |
| 热门分区Top10 | `community_page_view` + `post_interaction` 按分区聚合 | ✅已有 | 按分区维度聚合互动数/帖子数/活跃用户 |
| 热门话题Top10 | `post_interaction` + `post_click` 按话题聚合 | ✅已有 | 按话题维度聚合互动数/帖子数/浏览量 |

### 社区模块布局说明

- 第一行：活跃趋势（全宽）
- 第二行：互动趋势 + 分类型互动曲线
- 第三行：热门分区Top10 + 热门话题Top10

### 社区模块缺口（需新增）

| 指标 | 缺失说明 | 建议新增事件 |
|------|---------|------------|
| 社区人均停留时长 | 社区首页PRD没有定义 `community_page_stay` 事件 | 🆕 `community_page_stay`：触发=离开社区页时上报；参数=`stay_duration`(ms), `scroll_depth`(int), `uid` |
| 帖子详情停留时长 | 需要计算人均阅读深度 | 🆕 `post_detail_stay`：触发=离开帖子详情时上报；参数=`post_id`, `stay_duration`(ms), `scroll_depth` |

---

## 五、核心功能漏斗

> 统一口径说明：启动相关步骤统计口径为设备数(device_id去重)，其余步骤为UV(user_id去重)。
> 展示形式：从横向条形图改为表格形式。

### 5.1 登录率漏斗

| 漏斗步骤 | 所需事件 | 状态 | 说明 |
|---------|---------|------|------|
| APP启动设备数 | `app_launch` 去重 device_id | ✅已有 | — |
| 登录弹窗曝光 | `login_popup_view` | ✅已有 | 6.0.9#6 |
| 登录方式选择 | `login_method_select` | ✅已有 | 6.0.9#7 |
| 登录成功 | `login_result` (is_success=true) | ✅已有 | 6.0.9#9 |
| 各登录方式用户数 | `login_result` 按 method 分组 | ✅已有 | method枚举: sim_one_click/phone_sms/wechat/qq/google/guest |
| APP登录率 | `login_result`(success) / `app_launch` | ✅已有 | 计算指标 |
| Steam绑定数 | `steam_login_result` (is_success=true) | ✅已有 | bi-events + 6.0.9#14 |
| Steam绑定率 | steam绑定数 / 登录用户数 | ✅已有 | 计算指标 |
| 最后一次绑定/未绑定Steam用户数 | 需按 user_id 取最后一次 `steam_login_result` 状态 | ✅已有 | 后端按 user_id 最新记录聚合 |

### 5.2 启动率漏斗

| 漏斗步骤 | 所需事件 | 状态 | 说明 |
|---------|---------|------|------|
| APP启动设备数 | `app_launch` | ✅已有 | — |
| APP登录用户数 | `login_result` (success) | ✅已有 | 6.0.9#9 |
| Steam绑定用户数 | `steam_login_result` (success) | ✅已有 | — |
| Steam下载用户数 | `game_download_complete` (game_type=pc_steam, success) | ✅已有 | 6.0.9#16 |
| Steam启动用户数 | `game_start_click` (game_type=pc_steam) | ✅已有 | 6.0.9#19 |
| 秒玩启动用户数 | `cloud_game_enter` | ✅已有 | 6.0.9#12 |
| 秒玩启动率 | cloud_game_enter / 登录用户数 | ✅已有 | 计算 |
| Steam游戏启动率 | game_start_click(pc_steam) / Steam绑定用户数 | ✅已有 | 计算 |
| 边玩边下用户数 | 需标识"边下边玩"场景的启动 | ⚠️需改造 | 见下方 |
| 导入游戏启动率 | game_start_click(pc_import) / 有导入记录用户数 | ✅已有 | 6.0.9#19 game_type=pc_import |

### 5.3 AI插帧

| 漏斗步骤 | 所需事件 | 状态 | 说明 |
|---------|---------|------|------|
| 游戏详情页 | `game_detail_view` | ✅已有 | — |
| 进入游戏 | `game_play_success` | ✅已有 | — |
| 侧边栏唤起 | `hover_panel_view` | ✅已有 | bi-events「PC/Wine游戏内与会话」 |
| 性能Tab | `panel_tab_click` (tab_name=performance) | ✅已有 | bi-events |
| AI插帧开启/关闭 | `setting_toggle` (setting_id=ai_frame_interpolation) | ⚠️需确认 | bi-events有 `setting_toggle`，需确认 setting_id 枚举值是否包含 AI 插帧 |
| 最后一次开启/关闭用户数 | 按 user_id 取最新 `setting_toggle` 状态 | ⚠️同上 | 后端聚合 |

### 5.4 横竖屏切换

| 指标 | 所需事件 | 状态 | 说明 |
|------|---------|------|------|
| 切换次数 | `orientation_switch` | ✅已有 | bi-events「首页/探索」 |
| 最后一次横/竖屏设备数 | `orientation_switch` 按 device_id 取最新 to_orientation | ✅已有 | — |
| 最后一次横/竖屏用户数 | `orientation_switch` 按 user_id 取最新 to_orientation | ✅已有 | — |

### 5.5 云组队

| 漏斗步骤 | 所需事件 | 状态 | 说明 |
|---------|---------|------|------|
| 首页 | `explore_page_view` | ✅已有 | — |
| 玩游戏Tab | `play_page_view` | ✅已有 | bi-events |
| 组队大厅 | 需组队大厅页面曝光 | 🆕需新增 | 见下方 |
| 加入/创建房间 | 需操作事件 | 🆕需新增 | 见下方 |
| 组队游戏中 | 需组队游戏会话事件 | 🆕需新增 | 见下方 |

### 5.6 社区互动

| 漏斗步骤 | 所需事件 | 状态 | 说明 |
|---------|---------|------|------|
| 首页 | `explore_page_view` | ✅已有 | — |
| 社区首页 | `community_page_view` | ✅已有 | 社区PRD |
| 帖子详情页 | `post_click` | ✅已有 | 社区PRD |
| 互动(赞/评/转/发帖) | `post_interaction` + `post_publish_submit` | ✅已有 | 社区PRD |

### 5.7 PC引擎配置

| 漏斗步骤 | 所需事件 | 状态 | 说明 |
|---------|---------|------|------|
| 游戏详情页 | `game_detail_view` | ✅已有 | — |
| 唤起设置菜单 | `more_settings_click` | ✅已有 | bi-events |
| PC引擎设置页 | `setting_item_click` (item_name=pc_settings) | ✅已有 | bi-events |
| 推荐方案/调整参数 | `scheme_apply_click` | ✅已有 | bi-events + 6.0.9#27 |

### 5.8 边下边玩

| 漏斗步骤 | 所需事件 | 状态 | 说明 |
|---------|---------|------|------|
| 游戏详情页 | `game_detail_view` | ✅已有 | — |
| 触发边玩边下 | 需标识事件 | 🆕需新增 | 见下方 |
| 进入云游 | `cloud_game_enter` | ✅已有 | 6.0.9#12 |

---

## 六、流失用户分析（合并至"用户数据"模块的"流失分析"Tab）

| 看板指标 | 所需事件 | 状态 | 说明 |
|---------|---------|------|------|
| 日流失/7日累计 | `app_launch` 反向 | ✅已有 | 后端离线 |
| 流失前置事件Top10 | 全量事件按流失用户最后 session 聚合 | ✅已有 | 后端离线查询 |
| 流失用户画像对比（版本/渠道） | 公参中的版本/渠道/机型 | ✅已有 | — |
| 页面跳失率Top10 | 需页面级 view + 下一步事件判定 | ⚠️需改造 | 当前各页面 `*_page_view` 事件已有，但需后端定义"跳失"逻辑（进入页面后无后续交互事件即离开） |
| 卸载前/流失前行为还原 | 流失用户最后 session 事件序列 | ✅已有 | **注意：无法获取真实卸载时刻**，用"最后一次 session 的事件"近似 |

### 行为还原说明（更新）

- 区分维度：互动事件 / 停留页面
- 时间范围切换：最后一次 / 全部
- 表格列：排名 / 事件(页面)名称 / 次数 / 次数占比 / 人数 / 人数占比
- 默认按人数占比降序排列
- 新增：版本/渠道画像对比图（左侧版本分布饼图 + 右侧渠道分布饼图）

### 流失模块缺口

无新增客户端埋点需求。所有分析基于后端对已有事件的离线计算。

---

## 七、需新增埋点汇总

以下是看板需求中，经排查后确认**当前无法覆盖、且不在已有PRD排期中**的事件，需要补充：

| # | 事件名 | 触发时机 | 参数 | 所属模块 | 优先级 |
|---|--------|---------|------|---------|-------|
| 1 | `community_page_stay` | 离开社区首页时 | `stay_duration`(ms), `scroll_depth`(int), `current_partition`(string) | 社区平台 | P1 |
| 2 | `post_detail_stay` | 离开帖子详情页时 | `post_id`(string), `stay_duration`(ms), `scroll_depth`(int), `has_interaction`(bool) | 社区平台 | P1 |
| 3 | `team_lobby_view` | 进入云组队大厅页面 | `source_page`(string), `screen_orientation`(string) | 云组队漏斗 | P1 |
| 4 | `team_room_action` | 点击加入房间/创建房间 | `action_type`(string: join/create), `game_id`(string), `room_id`(string) | 云组队漏斗 | P1 |
| 5 | `team_game_start` | 组队游戏成功开始（所有玩家就位） | `game_id`(string), `room_id`(string), `player_count`(int), `wait_duration_ms`(long) | 云组队漏斗 | P1 |
| 6 | `play_while_download_trigger` | 触发边玩边下（检测到游戏未下载完但支持云游） | `game_id`(string), `game_type`(string), `download_progress`(float: 0-1), `source_page`(string) | 边下边玩漏斗 | P1 |

---

## 八、需改造/追加参数汇总

| # | 现有事件 | 需追加/变更 | 说明 | 优先级 |
|---|---------|-----------|------|-------|
| 1 | 公参链路 | 追加 `chip_model`(string, 规范化芯片型号) | 支撑"硬件类型数量"指标 + 流失画像机型对比 | P1 |
| 2 | 公参链路 | 追加 `device_model`(string, 规范化机型名) | 同上 | P1 |
| 3 | 公参链路 | 追加 `system_language`(string, 如 zh-CN) | 支撑"系统语言分布"饼图 | P2 |
| 4 | 公参链路 | 追加 `country_code`(string, 如 CN/US) | 支撑"国家/地区分布"饼图 | P2 |
| 5 | 公参链路 | 追加 `account_type`(string: registered/guest/anonymous) | 替代 boolean login_status，精确区分游客和匿名 | P1 |
| 6 | `setting_toggle` | 确认 `setting_id` 枚举值包含 `ai_frame_interpolation` | 支撑 AI 插帧漏斗 | P1 |

---

## 九、后端离线计算任务（无需客户端埋点，需数据团队排期）

| # | 任务 | 输入事件 | 输出 | 说明 |
|---|------|---------|------|------|
| 1 | 应用流失标记 | `app_launch` | 每日标记连续15天无启动的 device_id 为流失 | — |
| 2 | 留存矩阵计算 | `app_launch` | 按日期 × (1/3/7/14/30)日 生成留存率矩阵 | 区分新/老用户 |
| 3 | 页面跳失率 | 全量 `*_page_view` + 后续事件 | 按页面计算 bounce rate | 定义：进入页面后60秒内无后续交互事件 |
| 4 | 流失前置事件 | 全量事件 | 取流失用户最后一次 session 内的事件分布 | 区分互动事件/停留页面，按人数占比排序 |
| 5 | 芯片/机型字典映射 | 设备上报原始字符串 | 清洗映射表（如 SM8550-AB → 骁龙8 Gen2） | 需维护更新 |
| 6 | session 时长计算 | `app_launch` + 任意事件 | session = 同一 device_id 的 app_launch 到最后事件时间差 | 超过30分钟无事件视为 session 结束 |
| 7 | 云游营收日聚合 | `pay_result` + `cloud_game_enter` | 订单数/金额/ARPPU/新增付费用户 + 环比同比 + 日均 | — |
| 8 | 社区互动+话题聚合 | `post_interaction` + `post_click` | 互动按类型日聚合 + 分区Top10 + 话题Top10 | — |

---

## 十、已有事件覆盖度统计

| 模块 | 指标总数 | ✅已覆盖 | ⚠️需改造 | 🆕需新增 |
|------|---------|---------|---------|---------|
| 工作台总览 | 18 | 15 | 0 | 3(公参) |
| 用户生命周期 | 8 | 7 | 1 | 0 |
| 游戏生态 | 13 | 13 | 0 | 0 |
| 社区平台 | 10 | 8 | 0 | 2 |
| 核心功能漏斗 | 28 | 22 | 2 | 4 |
| 流失分析 | 5 | 4 | 1 | 0 |
| **合计** | **82** | **69 (84%)** | **4 (5%)** | **9 (11%)** |

> 结论：84%的指标已有埋点覆盖，需新增的主要集中在社区停留时长和云组队/边下边玩漏斗。公参追加（芯片/机型/语言/地区/账户类型）影响面广但改动集中在一个位置。
