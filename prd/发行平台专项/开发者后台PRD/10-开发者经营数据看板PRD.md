# 【PRD】PC 发行平台开发者经营数据看板需求

## 修订记录

| 修订日期 | 修订内容 | 版本 | 修订人 |
|---|---|---|---|
| 2026/9/11 | 精简数据看板页头；补充站内曝光、访问、购买、下单、获取、履约漏斗及来源分析；拆分公共与交易筛选口径 | V1.3 | 郑群超 |
| 2026/9/11 | 将经营数据入口收敛到单游戏控制台；固定当前游戏并移除游戏筛选；补充游戏归属校验和财务流水跳转契约 | V1.2 | 郑群超 |
| 2026/9/11 | 建立经营概览、脱敏订单明细、预估收入与待结算方案；统一履约、多币种和财务跳转契约 | V1.1 | 郑群超 |

**备注：** 搜2026.9.11修改。

## 一、文档概述

### 1.1 背景概述

- **需求背景：** 已完成企业认证的游戏开发者和发行商需要查看自己游戏从站内曝光到履约的转化，以及销量、退款、拒付、预估收入和待结算结果。
- **当前方式：** 单游戏“经营数据”已有交易结果、脱敏订单和财务摘要；开发者只能看到最终销量，无法判断变化来自流量、详情承接、下单、支付还是履约。
- **问题：** 缺少站内转化和来源数据会使开发者无法定位流失环节，也无法判断首页推荐、搜索、排行榜或活动等位置带来的有效获取。
- **解决方案：** 在经营概览首屏补充“有效曝光→游戏卡点击→详情页访问→购买／领取点击→创建订单→成功获取→履约成功”及来源分析，交易和财务继续使用现有页签。

### 1.2 需求边界

| 边界类型 | 范围说明 |
|---|---|
| 产品边界 | 只改开发者后台 B 端：完善统一 02 Demo 单游戏“经营数据”的经营概览、指标口径和截图，保留订单明细、收入与结算及第十五份财务模块跳转。平台级侧边栏仍只保留“游戏管理”“厂商设置”；不修改 C 端页面。 |
| 业务边界 | 只统计买断游戏本体与永久 DLC；平台只覆盖 Android、Mac；履约只覆盖直接购买、CDKEY。预约、评分评价、社区互动、广告投放深度分析、排行榜名次分析和平台平均转化率不在本需求内；排行榜只可作为站内来源。 |
| 运营边界 | 运营负责维护数据口径版本并处理异常数据；本需求不新增运营配置页面，不允许运营在该看板修改订单、退款、拒付或结算终态。 |
| 人力边界 | 服务端按厂商权限返回聚合指标、脱敏订单和账单摘要；Web 前端实现查询、页签、详情和财务跳转；数据团队校验指标与埋点；财务系统提供正式账单状态；测试覆盖权限、口径、状态和响应式表现。 |

### 1.3 术语定义

| 术语／缩写 | 英文全称（按需） | 定义与判断口径 |
|---|---|---|
| 直接购买 | Direct Purchase | 用户付款后，平台直接向购买账号发放游戏或 DLC 永久权益；内部履约枚举固定为 `account_entitlement`。 |
| CDKEY | Content Delivery Key | 用户下单后获得可一键激活的 Key；开发者侧只记录 Key 已交付，不展示 Key 明文。 |
| 业务来源 | `ledger_source` | 财务流水的业务归属，枚举为 `direct_sale`（平台直销）、`external_key`（外部 Key 采购）、`gamehub_key`（盖世 Key 渠道）；它与履约方式分开记录。 |
| 履约订单队列 | Fulfilled Order Cohort | 按筛选期内的履约完成时间选出订单，并以同一数据截止时间读取这些订单的退款和拒付当前结果；付费销量、退款率和净销量必须使用同一队列。 |
| 有效曝光 UV | Qualified Impression UV | 当前游戏或独立 DLC 素材满足可见时长与面积规则后的去重访问者数；具体有效曝光阈值沿用全站统一埋点规范。 |
| 入口队列 | Entry Cohort | 筛选期内首次发生有效曝光或详情页访问的访问者集合；后续 7×24 小时内同一游戏或 SKU 的行为归入该入口。 |
| 成功获取 UV | Successful Acquisition UV | 支付成功或 0 元领取成功的去重用户数；不代表已完成履约、付费销量或收入。 |
| 来源归因 | Source Attribution | 成功获取采用 7×24 小时内最后一次有效非直接触点；无可识别触点时归为自然直达，归因结果在成功获取时冻结。 |
| 付费销量 | Paid Sales | 应付金额大于 0 且完成履约的订单数。直接购买以账号权益生效为准，CDKEY 以 Key 成功交付为准。 |
| 免费领取 | Free Claim | 应付金额等于 0 且完成履约的订单数。 |
| 净销量 | Net Sales | 同一履约订单队列中的付费销量减去截至数据截止时间已完全退款或拒付败诉的订单数；同一订单只扣一次。 |
| 拒付 | Chargeback | 支付机构发起的争议扣款。待裁决只计风险，败诉后才扣减收入。 |
| 预估收入 | Estimated Revenue | 当前筛选范围内按销售额和逆向交易、费用动态计算的非正式收入。 |
| 待结算 | Pending Settlement | 第十五份财务模块返回的当前厂商已锁定结算单未清偿余额；在单游戏经营数据页中作为厂商账单摘要展示，仅随当前厂商、账期和结算币种变化，不按当前游戏及其他经营条件重新分摊。 |
| 对账流水号 | Reconciliation ID | 平台为一笔交易生成的稳定核账编号；可供开发者沟通定位，但不能反查玩家身份。 |
| 用户端已删除 | Deleted from User List | 玩家仅从自己的订单列表移除记录；不改变交易状态、经营统计和财务对账。 |

## 二、产品说明

### 2.1 产品／方案简介

| 项目 | 说明 |
|---|---|
| 产品／方案定位 | 面向发行平台开发者的单游戏只读数据入口，串联站内发现、交易获取、履约结果、脱敏订单和财务摘要。 |
| 目标用户 | 已完成企业认证、拥有当前厂商经营数据权限且有权管理当前游戏的开发者、发行商及财务人员；资格暂停账号可查看授权范围内的历史只读数据。 |
| 核心目标 | 让开发者定位曝光到履约的主要流失环节，判断来源质量，解释销量及逆向交易变化，并区分预估收入与正式结算。 |
| 使用场景 | 日常观察站内转化和来源、核查销量与免费领取、定位退款或拒付订单、月度结算前预核。 |
| 功能概述 | “经营概览”先展示七步站内转化、来源分析和趋势，再展示交易经营结果；“订单明细”和“收入与结算”继续承接订单定位与财务摘要。当前游戏固定，公共筛选与交易筛选分区生效。 |

### 2.2 产品流程

![数据看板流程](https://cdn.jsdelivr.net/gh/z36358631-ship-it/-@72c4a97901c9e1c938e161d47f6184889e51fa26/public/prd/publisher-data-dashboard/01-product-flow.png)

*图 2.2-1：开发者查看站内转化与来源，结合交易结果定位异常订单，再进入正式对账。*

流程说明：

1. 企业开发者从“游戏管理”进入有权管理的目标游戏，再点击“经营数据”，默认查看该游戏近 30 天站内转化。
2. 开发者按时间、商品、来源位置、地区和平台查看七步漏斗、趋势及来源差异，再结合付费销量、免费领取、退款、拒付、预估收入和待结算判断经营结果。
3. 需要定位交易时进入“订单明细”，按公共条件、履约方式、订单状态或编号筛选并查看脱敏详情。
4. 开发者进入“收入与结算”核对动态预估和锁定待结算摘要；需要正式处理时跳转第十五份财务模块。

## 三、功能需求

### 3.1 B 端功能需求

#### 3.1.1 P10-01 经营概览

| 要素 | 内容说明 |
|---|---|
| 功能简介 | 查看当前游戏的站内转化、来源质量、交易规模、逆向风险和收入摘要。 |
| 场景描述 | 开发者进入目标游戏后判断近期销量变化来自流量、详情承接、下单、获取还是履约，再决定是否定位订单或进入正式账单。 |
| 输入／前置条件 | 已登录；企业认证状态为“已通过”，当前账号拥有厂商经营数据权限且有权管理当前游戏。企业资格“已暂停”时，仅允许查看暂停前已授权游戏形成的历史数据，页面整体只读。 |
| 需求描述 | **图示（1张）：**<br><br>![经营概览](https://cdn.jsdelivr.net/gh/z36358631-ship-it/-@72c4a97901c9e1c938e161d47f6184889e51fa26/public/prd/publisher-data-dashboard/02-dashboard-overview.png)<br><br>*图 3.1.1-1：站内转化漏斗、来源分析与交易经营结果。*<br><br>**详细说明：**<br>**展示说明：**<br>1. 平台级侧边栏只展示“游戏管理”“厂商设置”；进入目标游戏后，单游戏功能依次为“版本发布”“发布记录”“资质认证”“经营数据”，当前项高亮。<br>2. 内容页头只显示标题“数据看板”；不重复显示眉题、游戏名称、Game ID 或副说明。当前游戏由面包屑表示；右侧保留“数据口径”“只读经营数据”和数据更新时间。<br>3. 页签依次为“经营概览”“订单明细”“收入与结算”，默认选中“经营概览”。公共筛选依次为时间、商品类型、来源位置、地区和平台；时间默认“近 30 天”，其余默认“全部”，不展示游戏、履约方式或订单状态筛选。<br>4. “从曝光到履约”依次展示有效曝光、游戏卡点击、详情页访问、购买／领取点击、创建订单、成功获取、履约成功。每步显示 UV、上一步转化率和环比；顶部展示“曝光→成功获取”总转化率。<br>5. “站内转化趋势”可查看有效曝光、详情页访问、购买／领取点击、成功获取的按日变化。“站内位置与访问来源”按首页推荐、找游戏、排行榜、搜索、专题活动、站外活动、自然直达、其他展示曝光 UV、点击 UV、详情访问 UV、成功获取 UV、点击率和详情到获取转化率。自然直达无曝光分母时显示“--”。<br>6. “交易经营结果”依次展示付费销量、免费领取、退款成功、拒付风险、预估收入、待结算；下方保留成交与领取趋势、商品与履约、退款与拒付。待结算标明厂商账单口径，不作单游戏分摊。<br>**交互说明：**<br>1. 开发者切换时间、商品类型、来源位置、地区或平台后，系统固定当前 Game ID，以同一数据截止时间刷新漏斗、来源、趋势和可归因交易结果；DLC 只统计独立卡片、模块或详情产生的上游事件，不分摊游戏本体曝光。<br>2. 点击趋势指标后，页面只切换趋势序列，保留其他筛选和滚动位置。点击“重置”恢复默认公共筛选；查询失败时保留条件并允许重试，不用 0 代替未知值。<br>3. 点击“数据口径”打开只读弹窗，展示入口队列、来源归因、UV 去重、交易时间口径、销量、逆向交易、收入、待结算和脱敏范围；关闭后保留页面上下文。<br>4. 点击“订单明细”或“收入与结算”切换页签并沿用公共筛选；履约方式和订单状态仅在这两个交易页签显示和生效，不反向过滤站内上游事件。 |
| 输出／后置条件 | 页面形成一份带更新时间和口径版本的经营快照；开发者可继续定位订单或查看收入与结算。 |
| 补充说明 | 1. 服务端从登录会话确定厂商，并校验 Game ID 归属和账号权限；不接受前端传入其他厂商编号或越权 Game ID。<br>2. 个人、未认证、审核中、已驳回、无厂商数据权限或无当前游戏权限的账号不展示“经营数据”；直接访问时回到游戏管理且不泄露记录是否存在。<br>3. 漏斗统一使用 UV，交易区使用订单数和金额；分母为 0、字段缺失、批次延迟或阶段不适用时显示“--”及原因。来源分组少于 10 UV 时显示“<10”，并隐藏比率或合并到“其他”。<br>4. 漏斗按入口时间形成队列并在 7×24 小时内归因；销量按履约完成时间形成订单队列。退款和拒付不回写历史转化，只影响净销量与收入。<br>5. CDKEY 没有真实下载或站外激活回传时不推算后续数据；本期漏斗以成功交付为履约终点。<br>6. 订单金额保留原币；收入按汇率版本折算到结算币。必要费用缺失时显示“估算中／--”；多个结算币种分别展示，禁止相加。 |

##### 角色权限与数据范围

| 账号状态／角色 | 权限与范围 | 禁止与越权 | 历史与恢复 |
|---|---|---|---|
| 企业认证已通过且有经营数据与当前游戏权限 | 查看当前游戏聚合指标和脱敏订单，并查看厂商结算摘要 | 不可跨游戏查询，不可修改订单、退款、拒付或账单终态 | 任一权限撤销后立即停止新查询 |
| 企业财务权限且有当前游戏权限 | 查看当前游戏经营数据，并可从看板进入有权访问的财务模块 | 看板内不能确认账单、提交差异、处理发票或付款 | 财务操作日志由第十五份模块记录 |
| 企业资格已暂停 | 查看暂停前已授权游戏形成的历史只读数据 | 不可跨游戏查询，不可创建、编辑、提审或执行财务操作 | 资格恢复后按最新授权恢复查询 |
| 个人、未提交、审核中、已驳回、无厂商授权或无当前游戏权限 | 不展示真实经营数据 | 服务端统一拒绝越权查询，不返回游戏或订单是否存在 | 完成企业认证并获得对应授权后生效 |

#### 3.1.2 P10-02 订单明细

| 要素 | 内容说明 |
|---|---|
| 功能简介 | 查询当前游戏的脱敏交易记录，并查看履约结果和资金影响。 |
| 场景描述 | 开发者发现销量、退款或拒付异常，或需要用稳定编号与平台支持、财务人员核对一笔交易。 |
| 输入／前置条件 | 已满足厂商经营数据与当前游戏查看权限；从当前游戏“经营数据—订单明细”进入。当前筛选条件沿用经营概览，订单编号默认空。 |
| 需求描述 | **图示（2张）：**<br><br>![订单明细](https://cdn.jsdelivr.net/gh/z36358631-ship-it/-@72c4a97901c9e1c938e161d47f6184889e51fa26/public/prd/publisher-data-dashboard/03-order-list.png)<br><br>*图 3.1.2-1：脱敏订单列表和组合筛选。*<br><br>![订单详情](https://cdn.jsdelivr.net/gh/z36358631-ship-it/-@72c4a97901c9e1c938e161d47f6184889e51fa26/public/prd/publisher-data-dashboard/04-order-detail.png)<br><br>*图 3.1.2-2：订单只读详情与资金影响。*<br><br>**详细说明：**<br>**展示说明：**<br>1. 筛选项依次为时间、商品类型、来源位置、地区、平台、履约方式、订单状态和订单编号；后 3 项只在交易页签出现。订单编号支持匹配完整对账流水号和脱敏平台订单号；页面明确“完整对账流水号可用于核账，平台订单号仅展示脱敏片段”。<br>2. 列表每行展示时间／订单、游戏／商品、履约方式、地区／平台、金额、状态和操作。游戏／商品包含游戏名、SKU、本体或永久 DLC；履约包含直接购买或 CDKEY 及履约结果；地区／平台包含国家或地区、Android 或 Mac、支付渠道；列表金额展示交易原币。<br>3. 状态样例必须覆盖交易完成、免费领取、退款处理中、已退款、拒付待裁决、拒付胜诉、拒付败诉、已关闭。“用户端已删除”为辅助标识，不能替代交易状态。<br>4. 详情以单层右侧抽屉展示，包含订单快照、履约与资金影响。可见字段为对账流水号、脱敏订单号、时间、游戏、SKU、商品、商品类型、地区、平台、渠道、履约方式、履约结果、订单状态、交易原币金额、结算币折算、汇率版本和当前资金影响。<br>5. 页面和详情均不展示玩家身份、设备、支付账号、支付凭证、Key 明文、退款原文、客服记录或内部处理材料。<br>**交互说明：**<br>1. 开发者输入订单编号或调整任一筛选后，系统固定当前 Game ID，查询命中全部条件的记录；无结果时保留筛选并显示空态，点击“重置”恢复默认条件。<br>2. 点击“查看详情”打开对应订单抽屉；服务端再次校验厂商归属、当前游戏归属和账号查看权限，校验失败时不打开详情且不返回订单是否存在。<br>3. 点击关闭、遮罩或按 Esc 关闭抽屉；关闭后保留页签、筛选和滚动位置。加载失败时抽屉保持关闭，列表状态不变并允许重试。<br>4. 用户端删除订单后，本页只增加“用户端已删除”辅助标识；销量、收入、退款、拒付、导出和对账结果保持不变。 |
| 输出／后置条件 | 开发者获得可用于核账的脱敏订单信息，或关闭详情返回原列表上下文。 |
| 补充说明 | 1. 一笔交易一行，对账流水号稳定且唯一；重复回调不得生成重复销量或重复订单行。<br>2. 支付成功但权益或 Key 未完成履约的订单不计付费销量，进入平台异常处理；已关闭且未履约订单不计销量和收入。<br>3. 退款与拒付同时发生时显示“核对中”，完全退款和拒付败诉不能重复扣减净销量或预估收入。 |

##### 订单状态与统计影响

| 状态 | 进入条件 | 页面表现与统计影响 | 后续结果 |
|---|---|---|---|
| 交易完成 | 金额大于 0 且权益生效或 Key 已交付 | 计入付费销量和销售额 | 后续退款或拒付不回写历史毛销量 |
| 免费领取 | 金额等于 0 且完成履约 | 计入免费领取，不计销售额 | 权益继续有效 |
| 退款处理中 | 已发起退款但渠道未确认成功 | 展示处理中，不计退款成功，不扣预估收入 | 成功后进入已退款；失败后回到原交易状态 |
| 已退款 | 渠道确认退款成功 | 计退款成功；按实际退款金额扣预估收入；完全退款扣净销量 | 迟到退款进入下一账期调整 |
| 拒付待裁决 | 支付机构已确认争议打开 | 展示风险金额，不扣预估收入和净销量 | 裁决后进入胜诉或败诉 |
| 拒付胜诉 | 支付机构确认平台胜诉 | 清除风险，保留收入和净销量 | 只读终态 |
| 拒付败诉 | 支付机构确认平台败诉 | 扣拒付金额和净销量 | 迟到结果进入下一账期调整 |
| 已关闭 | 未完成支付或履约，订单关闭 | 不计销量、销售额、收入和待结算 | 只读终态 |

#### 3.1.3 P10-03 收入与结算

| 要素 | 内容说明 |
|---|---|
| 功能简介 | 展示动态预估收入、拒付风险和锁定待结算摘要，并进入正式财务处理页面。 |
| 场景描述 | 开发者完成经营观察或订单定位后，预核本期收入构成，判断是否需要进入正式对账。 |
| 输入／前置条件 | 已满足厂商经营数据与当前游戏查看权限；从当前游戏“经营数据—收入与结算”进入。预估收入固定当前 Game ID，并沿用时间、商品、来源位置、地区、平台、履约方式和订单状态；待结算使用登录会话中的厂商及财务模块返回的账期、结算币种。 |
| 需求描述 | **图示（1张）：**<br><br>![收入与结算](https://cdn.jsdelivr.net/gh/z36358631-ship-it/-@72c4a97901c9e1c938e161d47f6184889e51fa26/public/prd/publisher-data-dashboard/05-income-settlement.png)<br><br>*图 3.1.3-1：预估收入构成、拒付风险和待结算摘要。*<br><br>**详细说明：**<br>**展示说明：**<br>1. 左侧“预估收入”逐项展示按财务主体结算币种折算的销售额、退款成功、拒付败诉、税费、渠道费、平台分成、调整项和最终预估收入；页面展示结算币种、汇率版本，并标明“随退款、拒付和费用更新，不作为正式付款依据”“以结算单为准”。<br>2. “拒付待裁决风险”单独展示风险金额和“尚未扣减”，不混入拒付败诉扣减。<br>3. 右侧“待结算”展示当前厂商与账期的未清偿金额、结算币种、结算单号、付款状态和更新时间；只汇总已锁定且尚未清偿的金额。存在多个结算币种时分币种展示，不生成跨币种合计。<br>4. 底部操作依次为“查看对账流水”“前往对账结算”。看板内不展示账单确认、差异申诉、发票和付款操作。<br>**交互说明：**<br>1. 切换经营筛选后，系统以同一订单队列重新计算预估收入和风险；任一必要费用或汇率缺失时显示“估算中／--”，不把缺失项按 0 计算。待结算保持财务账单口径，不随经营筛选重新分摊。<br>2. 点击“查看对账流水”进入 `15-开发者财务结算demo.html#/settlement/flows?game={current_game_id}&fulfillment={account_entitlement|cdkey|all}&range={7d|30d|90d}&ledger_source=direct_sale`；点击“前往对账结算”进入 `15-开发者财务结算demo.html#/settlement?statement={statement_id}&ledger_source=direct_sale`。<br>3. 两个目标页都从登录会话重新解析 `vendor_id`；对账流水目标页再次校验 `current_game_id` 的厂商归属和账号权限。URL 禁止传入或信任厂商编号。目标页只接收白名单参数；参数无效、越权或目标数据不可见时安全降级到默认列表，不提示记录是否存在。<br>4. 跳转失败时保留当前游戏、看板页签和筛选，按钮恢复可点，允许重试。资格暂停账号能否进入财务页继续按第十五份模块权限判断。 |
| 输出／后置条件 | 开发者看到当前经营筛选下的动态预估收入，以及独立财务账单口径的待结算摘要；或进入正式对账结算／对账流水页面。 |
| 补充说明 | 1. 预估收入＝销售额－成功退款－拒付败诉－税费－渠道费－平台分成＋调整项，不使用固定比例。<br>2. 已锁定结算单不可改写；锁单后到达的退款、拒付或费用调整进入下一账期调整项并关联原订单和原账单。<br>3. 待结算以第十五份财务模块返回的账单／付款未清偿余额为唯一事实来源；部分付款只保留剩余金额，已完成、已结转或已取消金额不再计入。<br>4. 待结算只跟随厂商、账期和结算币种，不跟随游戏、商品、履约方式、地区、平台或订单状态筛选。 |

##### 经营看板与财务模块边界

| 对象 | 数据看板展示 | 财务模块处理 | 一致性要求 |
|---|---|---|---|
| 预估收入 | 按当前交易快照动态计算 | 不作为付款依据 | 使用相同原币、汇率版本、结算币种和规则版本 |
| 待结算 | 按结算币种展示财务模块返回的已锁定且未清偿摘要 | 确认正式金额、部分付款和付款状态 | 第十五份财务模块为唯一事实来源；经营筛选不得重新分摊 |
| 对账流水 | 展示订单级脱敏摘要并提供跳转 | 提供完整结算流水查询和导出 | 对账流水号、SKU、履约和资金方向一致 |
| 账单与付款 | 只展示摘要 | 处理确认、差异、发票、付款和凭证 | 看板不得修改正式状态 |

## 四、支撑及非功能需求

### 4.1 埋点与数据需求

**数据结论：** 新增开发者经营指标、页面使用事件和参数。

#### 4.1.1 指标口径

| 指标名称 | 口径（含公式、分子和分母） | 去重与时间窗口 | 数据源与决策用途 |
|---|---|---|---|
| 有效曝光 UV | 当前游戏或独立 DLC 素材满足全站统一有效曝光规则的访问者数 | 入口发生在筛选期内；登录用户优先按 `uid + game_id/SKU` 去重，未登录按合规匿名访问者标识去重 | 素材曝光事件；判断站内流量规模 |
| 游戏卡点击 UV | 有效曝光后点击当前游戏或独立 DLC 卡片的访问者数；曝光点击率＝游戏卡点击 UV ÷ 有效曝光 UV | 使用同一入口队列；按访问者与游戏／SKU 去重；分母为 0 时显示“--” | 卡片点击事件；判断素材吸引力 |
| 详情页访问 UV | 当前游戏详情成功展示的访问者数；卡片到详情率＝详情页访问 UV ÷ 游戏卡点击 UV | 入口队列后续 7×24 小时内归因；按访问者与游戏去重 | 详情展示事件；判断点击后的页面到达 |
| 购买／领取点击 UV | 点击“购买”或“免费领取”的访问者数；详情意向率＝购买／领取点击 UV ÷ 详情页访问 UV | 入口队列后续 7×24 小时内归因；按访问者与 SKU 去重 | 商品操作事件；判断购买意向 |
| 创建订单 UV | 成功创建游戏本体或永久 DLC 订单的用户数；下单率＝创建订单 UV ÷ 购买／领取点击 UV | 按用户与 SKU 去重；重复创建同一订单不重复计入 | 订单创建终态；判断交易链路进入率 |
| 成功获取 UV | 支付成功或 0 元领取成功的用户数；获取成功率＝成功获取 UV ÷ 创建订单 UV | 支付按 `payment_id` 首次成功去重，免费领取按 `claim_id` 成功终态去重 | 支付与领取终态；判断有效获取 |
| 履约成功 UV | 账号权益生效或 CDKEY 成功交付的用户数；履约完成率＝履约成功 UV ÷ 成功获取 UV | 按 `order_id + fulfillment_type` 成功终态去重，再按用户去重 | 履约终态；识别成功获取后的履约异常 |
| 曝光到获取转化率 | 成功获取 UV ÷ 有效曝光 UV | 使用同一入口队列、7×24 小时归因窗和数据截止时间；自然直达无曝光分母时显示“--” | 转化聚合；判断站内整体获取效率 |
| 付费销量 | 筛选期内应付金额＞0 且完成履约的订单数 | 先按 `fulfillment_completed_at` 选出履约订单队列，再按 `order_id + fulfillment_type` 去重 | 订单与履约；判断付费规模 |
| 免费领取 | 筛选期内应付金额＝0 且完成履约的订单数 | 与付费销量使用同一履约完成时间窗口，按 `order_id + fulfillment_type` 去重 | 订单与履约；判断免费转化规模 |
| 销售额 | 履约订单队列内付费订单的实付金额之和 | 支付按 `payment_id` 首次成功去重；交易原币按当前快照汇率版本折算为财务主体结算币种 | 支付、订单、履约；解释收入基数 |
| 退款成功 | 履约订单队列内，截至 `data_cutoff_at` 已完全退款的订单数与实际退款金额 | 订单数按 `order_id` 去重，金额按成功退款流水汇总；迟到退款仍进入后续正式账期调整 | 退款终态；识别队列内逆向交易 |
| 退款率 | 同一履约订单队列内完全退款订单数 ÷ 付费销量 | 分子、分母必须来自同一订单队列和同一数据截止时间；分母为 0 时显示“--” | 订单与退款；判断商品质量和售后风险 |
| 拒付风险 | 履约订单队列内，截至 `data_cutoff_at` 已打开且未裁决的拒付案例数与风险金额 | 按拒付案例号去重；只提示风险，不扣净销量和预估收入 | 支付机构拒付；预警潜在损失 |
| 拒付败诉 | 履约订单队列内，截至 `data_cutoff_at` 已败诉的订单数与金额 | 订单数按 `order_id` 去重；迟到裁决仍进入后续正式账期调整 | 支付机构拒付；确认队列内实际损失 |
| 净销量 | 同一履约订单队列内的付费销量－完全退款订单数－拒付败诉订单数 | 同一订单退款与拒付只扣一次；部分退款不扣销量 | 订单、退款、拒付；判断有效成交 |
| 预估收入 | 折算后销售额－成功退款－拒付败诉－税费－渠道费－平台分成＋调整项 | 以同一订单队列和 `data_cutoff_at` 计算；展示 `settlement_currency` 与 `fx_version`；缺少汇率或必要费用时为“估算中／--” | 交易、费用和规则快照；供结算前预核 |
| 待结算 | 第十五份财务模块返回的锁定账单未清偿余额 | 只按当前厂商、账期、结算币种读取；多个结算币种分别展示，禁止相加；不按经营筛选重新分摊 | 财务账单与付款；提示正式待处理规模 |

#### 4.1.2 埋点事件

| 事件 | 页面／类型 | 触发与成功 | 参数 |
|---|---|---|---|
| publisher_dashboard_view | B 端／经营概览／改造 | 权限校验完成且首屏数据返回后上报；每次进入一次，失败、空态或无权限分别记录结果 | vendor_id, qualification_status, dashboard_tab, time_range, game_id, product_type, source_position, region_scope, platform, settlement_currency, fx_version, attribution_window, data_cutoff_at, result |
| publisher_conversion_source_view | B 端／来源分析／新增 | 来源表完成展示后上报一次；无数据、批次异常或无权限分别记录结果 | vendor_id, game_id, time_range, product_type, source_position, region_scope, platform, attribution_window, data_cutoff_at, result |
| publisher_conversion_filter_change | B 端／公共筛选／新增 | 时间、商品、来源、地区或平台提交且转化数据刷新完成后上报；失败时保留旧数据并记录 error | vendor_id, game_id, time_range, product_type, source_position, region_scope, platform, filter_name, filter_value, attribution_window, data_cutoff_at, result |
| publisher_conversion_trend_metric_click | B 端／站内转化趋势／新增 | 点击趋势指标且对应序列成功展示后上报 | vendor_id, game_id, metric_name, time_range, product_type, source_position, region_scope, platform, result |
| publisher_dashboard_tab_click | B 端／数据看板页签／新增 | 点击页签且目标内容完成展示后上报；每次成功切换上报 | vendor_id, dashboard_tab, result |
| publisher_dashboard_filter_change | B 端／订单与收入筛选／新增 | 在订单或收入页提交履约方式、订单状态或订单编号并完成查询后上报 | vendor_id, dashboard_tab, time_range, game_id, product_type, source_position, fulfillment_type, region_scope, platform, order_status, filter_name, filter_value, result |
| publisher_order_detail_view | B 端／订单详情／新增 | 点击“查看详情”且抽屉成功展示后上报；越权或不存在记录为 denied | vendor_id, order_reference, result |
| publisher_data_scope_view | B 端／数据口径／新增 | 点击“数据口径”且弹窗成功展示后上报 | vendor_id, qualification_status, dashboard_tab, result |
| publisher_finance_redirect_click | B 端／财务跳转／新增 | 点击财务入口并完成权限校验、生成目标路由后上报 | vendor_id, finance_target, statement_id, time_range, game_id, fulfillment_type, ledger_source, result |

#### 4.1.3 参数说明

| 参数 | 类型／必填 | 说明 | 枚举／示例 |
|---|---|---|---|
| vendor_id | string／是 | 服务端从登录会话确定的厂商编号 | 例：VENDOR-STAR-001 |
| qualification_status | enum／是 | 当前企业认证资格状态 | approved＝已通过；delisted＝资格暂停 |
| dashboard_tab | enum／是 | 当前或切换后的页签 | overview＝经营概览；orders＝订单明细；revenue＝收入与结算 |
| time_range | enum／是 | 当前筛选时间范围 | 7d＝近 7 天；30d＝近 30 天；90d＝近 90 天；current_month＝本月 |
| game_id | string／是 | 当前单游戏控制台绑定的 Game ID；由页面上下文固定，不允许为 all | 例：GAME-48291 |
| product_type | enum／是 | 当前筛选商品类型 | all＝全部；base_game＝游戏本体；permanent_dlc＝永久 DLC |
| source_position | enum／是 | 当前来源筛选或归因位置 | all＝全部；home＝首页推荐；discovery＝找游戏；ranking＝排行榜；search＝搜索；campaign＝专题活动；external＝站外活动；direct＝自然直达；other＝其他 |
| fulfillment_type | enum／是 | 当前筛选履约方式 | all＝全部；account_entitlement＝直接购买／账号权益；cdkey＝CDKEY |
| region_scope | enum／是 | 当前筛选发行地区 | all＝全部；global_excl_mainland＝全球且不含中国大陆；mainland_china＝中国大陆 |
| platform | enum／是 | 当前筛选平台 | all＝全部；android＝Android；mac＝Mac |
| order_status | enum／是 | 当前筛选订单状态 | all＝全部；completed＝交易完成；free_claim＝免费领取；refund_pending＝退款处理中；refunded＝已退款；chargeback_open＝拒付待裁决；chargeback_won＝拒付胜诉；chargeback_lost＝拒付败诉；closed＝已关闭 |
| filter_name | enum／是 | 本次发生变化的筛选项 | time_range＝时间；product_type＝商品；source_position＝来源；fulfillment_type＝履约；region_scope＝地区；platform＝平台；order_status＝状态；order_reference＝订单编号 |
| filter_value | string／是 | 本次筛选提交值；订单编号上报不可逆摘要，不上报用户输入原文 | 例：30d、mac、sha256:8f2c… |
| metric_name | enum／是 | 当前趋势指标 | impression_uv＝有效曝光；detail_view_uv＝详情访问；cta_click_uv＝购买／领取点击；acquisition_success_uv＝成功获取 |
| attribution_window | enum／是 | 站内转化归因窗口 | 7d＝入口发生后 7×24 小时 |
| order_reference | string／是 | 订单详情的内部对账流水号或其不可逆摘要，不含玩家标识 | 例：RCN-202609-A8F2K7 |
| finance_target | enum／是 | 财务跳转目标 | settlement＝对账结算；settlement_flows＝对账流水 |
| statement_id | string／否 | 跳转对账结算时的结算单号；对账流水跳转不传 | 例：STMT-2026-06-V1 |
| ledger_source | enum／是 | 财务流水业务来源；与履约方式分离 | direct_sale＝平台直销；external_key＝外部 Key 采购；gamehub_key＝盖世 Key 渠道（本看板跳转固定为 direct_sale） |
| settlement_currency | string／否 | 财务主体的结算币种 | 例：USD、CNY |
| fx_version | string／否 | 聚合金额使用的汇率快照版本 | 例：FX-20260910-01 |
| data_cutoff_at | datetime／是 | 当前经营快照读取行为、交易和逆向结果的截止时间 | 例：2026-09-10 23:59:59+08:00 |
| result | enum／是 | 页面、查询、详情或跳转结果 | success＝成功；empty＝空结果；error＝失败；denied＝无权限 |

### 4.2 技术需求

| 需求项 | 具体要求 |
|---|---|
| 数据计算 | 服务端分别生成入口队列和履约订单队列，并在同一数据截止时间返回结果；前端只展示指标、更新时间和口径版本。金额使用整数最小单位或 Decimal，不能使用二进制浮点数。 |
| 身份去重 | 行为 UV 优先使用登录 `uid`，未登录使用合规匿名访问者标识；登录前后仅在取得合法关联依据时合并。事件次数和订单数不得作为 UV；来源分组触发隐私阈值时不得返回可识别明细。 |
| 归因 | 入口发生在筛选期内，后续同一游戏或 SKU 行为使用 7×24 小时窗口；成功获取采用最后一次有效非直接触点并冻结。自然直达从详情访问开始；游戏本体曝光不得分摊给 DLC。 |
| 权限 | 服务端从登录会话解析 `vendor_id` 和数据权限，同时校验 `game_id` 属于当前厂商且账号有权管理；不信任 URL、前端筛选或请求体中的厂商编号。聚合、列表、详情和财务跳转分别校验厂商与游戏权限。 |
| 一致性 | 同一筛选操作返回同一数据截止时间。履约方式和订单状态只过滤交易数据，不过滤曝光等上游行为；看板与财务模块使用相同订单、流水号、结算单号、原币、汇率版本、结算币种和规则版本。 |
| 跳转契约 | `#/settlement` 仅接收 `statement`、`ledger_source`；`#/settlement/flows` 仅接收固定当前 `game`、`fulfillment`、`range`、`ledger_source`。`vendor_id` 不进入 URL，由目标页从会话重新解析；`game` 必须为当前游戏的稳定 Game ID，不允许为 `all` 或游戏名称。目标页对白名单外、枚举非法、无权限或不可见数据统一降级到默认列表，不返回数据是否存在。 |
| 安全 | 列表、详情、日志和埋点不得包含玩家身份、设备、支付账号、支付凭证、Key 明文、客服记录和内部处理材料；越权结果不得泄露记录存在性。 |
| 兼容 | 支持发行平台桌面 Web 页面，并保证 390px 窄屏无页面根节点横向溢出；宽表仅在表格容器内横向滚动。 |
| 异常恢复 | 接口失败时保留页签、筛选和滚动位置；重试不重复生成销量或流水；跳转财务失败时保留当前页面。 |

### 4.3 运营需求

| 需求项 | 具体要求 |
|---|---|
| 口径维护 | 运营与数据团队维护可追溯的数据口径版本、更新时间和变更说明；口径变化不能静默改写已锁定账单。 |
| 异常协同 | 支付成功未履约、退款拒付冲突、汇率或必要费用缺失进入平台异常队列；开发者侧只展示可理解状态，不暴露内部处理材料。 |
| 客服定位 | 客服和财务使用对账流水号定位订单；不得要求开发者提供玩家隐私、支付凭证或 Key 明文。 |

### 4.4 财务、法务、版权与合规需求

| 需求项 | 具体要求 |
|---|---|
| 财务 | 看板金额为动态预估；正式金额、账单确认、差异、发票、付款和凭证以第十五份财务模块为准。待结算未清偿余额也以该模块为唯一事实来源。已锁定账单不可改写，迟到退款、拒付和费用进入下一账期调整并关联原订单与原账单。 |
| 法务 | 本需求不新增协议或资质；企业认证失效时按权限规则停止新数据查询，资格暂停账号仅保留授权的历史只读范围。 |
| 版权 | 页面只展示当前厂商有权管理的游戏名称、商品和素材；不得跨厂商返回其他主体数据。 |
| 其他合规 | 跨境与多币种交易保留原币、汇率版本和结算币种；页面及导出遵循数据最小化和订单脱敏要求。 |

## 五、待确认项

无。

## 六、附录

### 6.1 参考文档

| 文档名称 | 文档链接／位置 | 说明 |
|---|---|---|
| 开发者财务主体与对账结算 PRD | `prd/发行平台专项/开发者后台PRD/15-开发者财务主体与对账结算PRD.md` | 正式账单、流水、差异、发票和付款边界 |
| 数据看板设计说明 | `docs/superpowers/specs/2026-09-11-publisher-data-dashboard-design.md` | 本需求范围、页面结构和指标口径来源 |
| TapTap 开发者中心 | `https://developer.taptap.cn/` | 参考其单游戏商店数据中的素材曝光、点击、转化和位置筛选结构；不照搬预约、下载、评分或社区口径 |

### 6.2 原型／Demo 索引

| 原型／Demo 名称 | 链接／位置 | 说明 |
|---|---|---|
| 统一 02 游戏创建与发行 Demo | `demos/开发者后台一期/02-游戏创建与发行demo.html#/P02-01` | 右下角“Demo 状态”选择企业认证“审核通过”→进入“星海远征”→点击“经营数据”，体验站内转化、来源、三个页签、订单状态和财务跳转 |
| PRD 原图目录 | `public/prd/publisher-data-dashboard/` | 产品流程、经营概览、订单列表、订单详情和收入结算原图 |

> 图片状态：仅完成公网验证，飞书转存未验证。
