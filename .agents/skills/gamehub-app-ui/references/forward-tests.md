# 前向测试与当前严格状态

测试日期：2026-08-14。逻辑测试检查 Skill 是否正确路由、保留范围和输出证据；视觉测试直接读取 `assets/visual-report.json`，不得手填 PASS。

## FT-01 游戏库新增 GOG

- 输入：游戏库首页横竖两版，只在 EPIC 与导入游戏之间增加 GOG。
- 预期：GOG 插入指定位置；保留其他入口；覆盖未绑定、同步、失败、空库和有游戏；横竖独立布局。
- 禁止：擅自增加其他授权流程、弹窗或入口。
- 逻辑路由：PASS。
- 视觉结论：目标 Demo 生成后必须单独回归，不能复用本 Skill 预览分数。

## FT-02 我的页 GOG 账号卡

- 输入：GOG 已绑定账号卡，低频操作收进 `…`，不显示账号价值。
- 预期：主卡只展示账号、绑定/同步状态和游戏摘要；切换/解绑位于更多菜单；账号价值无字段、无占位。
- 异常状态：未绑定、同步中、同步失败、账号冲突。
- 逻辑路由：PASS。
- 视觉结论：目标 Demo 生成后单独回归。

## FT-03 搜索结果横竖版

- 输入：同一游戏 EPIC/GOG 分条，GOG 无评分显示“暂无评分”。
- 预期：竖屏一排 2 张，平台标识在封面；横屏为独立结果布局；共享数据但不整体缩放；缺失评分不显示 0。
- 逻辑路由：PASS。
- 视觉结论：平台原始 SVG 当前 `missing-source`，未取得前不得宣称完整视觉 PASS。

## FT-04 掌机横屏详情

- 输入：在“获得游戏”区域 Steam 旁增加 GOG，不新增 GOG 启动、云存档或时长。
- 预期：保留 screen-44 沉浸结构；只增加获取平台标识；不扩展能力。
- 逻辑路由：PASS。
- 视觉结论：目标 Demo 生成后单独回归。

## FT-05 离线页面事实库与组件展厅

- 输入：全部页面、导航、账号卡、游戏卡、按钮、弹层、加载/空/失败和来源。
- 结果：页面事实 45/45；组件目录 16；离线单文件；无 CDN、iframe、Canvas、远程图片或 Emoji 图标。
- 组件展厅：左栏原稿证据，右栏 DOM/来源化媒体；原稿不标为实现。
- 交互：页面搜索/筛选、组件状态切换、回归证据五种视图可操作。
- 结构与运行：PASS。
- 严格视觉：FAIL / 待人工审图，以 `assets/visual-report.json` 为准。

## 当前严格基准

### epic-unbound-portrait

- 画布：1080×2400；几何 4/4。
- 整页结构已超过 95%，但页面仍因登记组件和人工状态失败。
- `C-TOPBAR`：自动 PASS。
- `C-FEEDBACK`、`C-BUTTON-GLOW`、`C-NAV-P`：自动 FAIL，继续看组件差异图。
- 人工审图：`pending`。

### library-landscape

- 画布：2400×1080；几何 3/3。
- 整页结构已超过 95%，但组件独立门槛未全部通过。
- `C-NAV-L`、`D-GAME-CARD`、`C-BUTTON-SECONDARY`：自动 FAIL。
- 人工审图：`pending`。

## 判定

- 来源覆盖：Figma 11/11；实机页 45/45。
- 页面事实库：PASS。
- 组件目录与原稿/实现分栏：PASS。
- 离线运行与交互：PASS；`validate.mjs all` 已验证 0 页面错误、0 远程请求。
- 严格视觉：FAIL，禁止描述为“95% 已完成”。
- 只有 `visual-report.json` 的 `automaticPassed:true` 且人工状态为 `pass` 后，才可更新本结论。
